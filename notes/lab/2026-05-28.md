---
date: 2026-05-28
session_id: 2026-05-28-a
focus:
  - sir-helper-build-and-recovery-gate
  - q1a-prime-phase_a-methodology-amendment
  - q1a-z-conditioning-build-run-arbiter
  - q1a-settled-r-b1a-s12
  - thread-coordinator-s3-q1a-to-q1b
  - q1a-prime-deferred-fastica-design-defect
commits:
  - 78b5069 SIR helper sliced_inverse_regression_fit + recovery gate + Q1A' phase_a amendment (carve-out)
  - c3a62c2 experiment/q1a_z_conditioning — build Q1A script (head-to-head Z_b/Z_c, hard-assertion reproduction check)
  - b133647 experiment/q1a_z_conditioning — Check P L1 fix (per_cell_chi2 fields) + L2 advisory fix (landslide structured field)
  - 76b00e1 Q1A SETTLED R-B1A (S12) — hour_of_week Z-conditioning closes 0.18% of gap
preregistrations_touched:
  - 2026-05-27_q1a-prime-sdr-ica-sibling (phase_a methodology amendment §baselines.secondary; references re-stamped)
  - 2026-05-27_q1a-z-conditioning-strongest-axis (build, audit, run, arbiter, settled)
  - 2026-05-27_resolution-paths-thread (S3 node-advance Q1A→Q1B; Z_c carryforward)
memory_files_touched:
  - mitacs-q1a-z-conditioning.md (created; S12 entry)
  - MEMORY.md (S12 line added)
writeups_touched: []
cross_programme_refs:
  - ~/Research/Mathematics/Resolvent_Framework/notes/unsorted/disintegration_diagnostic.md (cited for joint S9+S10+S11+S12 framework reading)
settled_this_session:
  - 2026-05-27_q1a-z-conditioning-strongest-axis
dispatched_this_session: []
deferred_to_next_session:
  - "Fill Q1B phase_a — Z2 from time_of_day (P1's 2nd-strongest axis, range 0.485); cumulative chi^2 computed against Q1A's Z_c-conditioned baseline (with near-tie caveat); 1000 paired-day bootstrap CIs"
  - "Q1A' FastICA design decision (deferred per S12 session) — three advisor options on the table: (1) amend selection rule from max-|excess_kurtosis| to max-|Spearman ρ vs Y| (rule that composes with verdict statistic); (2) drop FastICA from head-to-head (Q1A' becomes SIR-only); (3) extend window. User pick required before re-stamping Q1A' phase_a a third time"
  - "ECCC temperature data acquisition (still on background list; per S12 reading, joint S9+S10+S11+S12 makes auxiliary-information arm increasingly likely needed) — pre-requisite for the EXHAUSTED-UNRESOLVED resolution path post-Q1B"
  - "Backfill (S11) + (S12) numerics into writeup/tex/missing_content_memo.tex when next touched (memo predates both)"
status_at_end: wrapped
---

# Session 2026-05-28-a — Q1A SETTLED R-B1A (S12), thread S3 → Q1B

## What happened

Continuation of session 2026-05-27-b's deferred items. The
resolution-paths-thread's gating Q1A node walked through end-to-end:
SIR helper build (for Q1A') → pre-run carve-out on Q1A' synthetic
gate → Q1A script build → audit chain → run → arbiter → thread S3
node-advance. Q1A' deferred mid-session per user direction after
empirical discovery that its FastICA selection rule does not compose
with its verdict statistic.

**Step A (SIR helper + recovery gate, commit 78b5069).** Built
`sliced_inverse_regression_fit(X, Y, n_slices=10)` in
`processing/innovations/estimator.py`, mirroring the `patra_sen_fit`
pattern. Generalized eigenproblem `Σ_b β = λ Σ_x β` via
`scipy.linalg.eigh` (the generalized form is necessary because
z-scored X has non-trivial off-diagonal correlations; a plain
eigendecomposition of Σ_b would land on a different basis).
Sanity-checked on linear-Y synthetic: top eigenvalue 0.76 dominant,
cos-similarity 0.9997 with true direction. Whitelisted in
`code_path.py` PRODUCTION_SYMBOLS. Added `sir_recovery()` +
`test_sir_recovery()` to `synthetic.py` gate suite.

**Step A-amend (Q1A' phase_a methodology amendment, same commit).**
First synthetic-gate run on Q1A''s registered cuts (rho ≥ 0.70 signal,
rho_null < 0.30) failed: rho_null = 0.47 in a realistic Ontario-mimic
DGM. After advisor consultation and multi-seed characterization
(p50=0.33, p95=0.54), the diagnosis was that the null cut is
unsatisfiable by construction when X carries the cyclic basis
(realistic Ontario lag1/lag24 demand IS weekly-periodic). Pre-run
carve-out (precedent: P1 F_b switch at commit 99a8122): SIR-side null
cut replaced with signal/null eigenvalue ratio ≥ 50× (SIR-native,
X-structure-invariant; empirical ratio ~225×). SIR-side signal floor
tightened 0.70 → 0.65 (worst-case Spearman phase ceiling). FastICA
gate cuts left unchanged pending separate testing. Q1A' phase_a
re-stamped (b14d09cb... → 610b5600...); proponent + DA references
bumped (forecast content untouched — they're on Ontario verdict cuts,
not synthetic-gate cuts).

**Step B (Q1A script build, commit c3a62c2).** Production-path
implementation:
- `mixture_2_gaussian_mle_fit` per (day_type, Z) cell — no
  reimplementation.
- Q2A helpers (`_build_library_for_daytype`,
  `_make_mixture_2_sampler`, `_marginal_pit_chi2`,
  `_smc_iterate_family`, `_pit_for_family`) imported from
  `experiment.distributional_class_q2a.__main__` and reused
  for the reproduction-check path.
- Hard-assertion reproduction check (advisor recommendation): M3-no-Z
  refit must reproduce Q2A's settled chi^2 = 953.84 within 50.0
  tolerance. Replays Q2A's exact M3 SMC seed
  (`SEED_BASE_Q2A + 100 + _TAG_SEED_OFFSET_M3 = 20260657`).
- Inline synthetic gate (BLOCKING): known Z-conditional DGP per
  phase_a §baselines.secondary; per-cell parameter recovery + per-cell
  test PIT chi^2 well below 60.0 ceiling; false-Z companion check.
- MIN_CELL_SIZE = 200 pool-to-parent fallback per phase_a §metric.
- CLAIM-GRADE provenance scaffolded via `make_result`.
Code-path audit: PRODUCTION-PATH (clean, 10 production imports + 5
legitimate Q2A helper reuses).

**Step B-fix (Check P L1 BLOCKING + L2 advisory fix, commit b133647).**
First phase-fidelity Check P found:
- L1 FAIL BLOCKING: two pre-registered dependent variables
  (`per_cell_chi2_contribution_Z_b/Z_c`) existed only in a coverage
  comment, not in the computation.
- L2 ADVISORY: landslide_R_A1A rendered in body but not as structured
  result.yaml field.
Both fixes mechanical:
- per-(day_type, Z) cell chi^2 decomposition via groupby on
  pit_tables[tag]; surfaced in body + result.yaml secondary_results
  + pickle payload.
- `landslide_R_A1A: bool | None` field added to primary_result.value
  (None when verdict ≠ R-A1A or no CI available; bool otherwise).
Re-audit: L1 PASS + L2 PASS.

**Step C (Q1A run, commit 76b00e1).** Background run with
`--emit-result`. Synthetic gate PASS (0.1s); reproduction-check exact
(Q1A no-Z chi^2 = 953.84, Q2A settled = 953.84, |diff| = 0.0;
SEED-EQUALITY clause holds); per-cell M3 fits per (day_type, Z) cell
(9.7s/9.9s); SMC propagation per candidate (~1.6s each); paired-day
bootstrap (33.7s, 1000 reps).

**Headline numerics (CLAIM artifact body_sha256 bf8eed24...):**
```
chi2_Z_b       = 986.02   CI [678.23, 1381.66]
chi2_Z_c       = 952.52   CI [626.97, 1373.91]
better_chi2    = 952.52   CI [625.34, 1342.33]
chi2_diff      = +33.50   CI [-93.16, +142.64]  (paired SE 56.84)
winner         = Z_c       (NEAR-TIE: |diff| < 1σ)
outcome        = R-B1A
```

**Step C-tail (Check R + arbiter + thread S3).** Phase-fidelity
Check R PASS (15/15 dependent variables + 6 additional items).
Arbiter rendered SETTLED R-B1A (body_sha256 6b49d913...). Thread-
coordinator S3 node-advance: Q1A active → settled, Q1B planning →
active, current_node_id Q1A → Q1B, Z_c carryforward. New thread
body_sha256 173b3d58..., prior_hash 21a8bb20... chained. All 13
registry entries chain verify.

## Decisions

- **Lock Q1A SETTLED R-B1A as the substantive negative result on
  the within-data Z lever.** Z conditioning shaved 1.33 chi^2 off
  Q2A's 953.84 baseline — 0.18% of the 725.84 gap. The R-B1A category
  understates this; the substantive picture is "hour_of_week
  σ-algebra enrichment delivers essentially no marginal reduction
  beyond bootstrap noise." Per-cell decomposition exactly corroborates
  P1 (saturday__bin6 contributes 1634 of the pooled marginal 952.5)
  but the mixture-2-Gaussian family is at capacity within strata.

- **Lock the joint S9+S10+S11+S12 framework reading: three within-data
  levers (richer distributional families, mechanism ablation,
  hour_of_week stratification) are all empirically bounded.**
  Resolution requires auxiliary information beyond the time index
  (temperature / exogenous Z). This is now applied chi^2-side
  corroboration of the identifiability obstruction parked at
  `disintegration_diagnostic.md`, not just from Patra-Sen α̂_L.
  The Goal-4 paper now carries an honest "auxiliary information
  required" reading as a publication-grade conclusion.

- **Lock the fifth named-failure-mode instance in a new shape.**
  Mechanical R-B1A matches DA; DA's Z_c point forecast (950 vs
  realized 952.52, 0.3% residual) is essentially EXACT — the
  strongest pre-registered point forecast in the workflow's history.
  But DA's WINNER direction was wrong (predicted Z_b by −150;
  actual Z_c by +33.5 at NEAR-TIE strength). Arbiter-pattern
  absorbs this without engineering changes.

- **Defer Q1A' (user direction).** Empirical discovery: phase_a
  §metric's FastICA selection rule (max |excess_kurtosis|) does
  not compose with the cyclic-ρ verdict statistic. Multi-seed test
  showed FastICA picks the saturday/sunday binary-indicator
  columns (signal/null kurt ratio ~1.0), giving bimodal rho_signal
  {~0.65, ~0.01} and rho_null at the X-cyclic floor 0.55-0.71 —
  the 0.70/0.30 cuts unsatisfiable. A third Q1A' phase_a amendment
  in one session would cross from "carve-out for real defect" into
  "rewriting the experiment as I build it." Per advisor + user,
  defer until empirical FastICA findings can inform clean re-design.

- **Lock thread state at current_node = Q1B with EXHAUSTED-UNRESOLVED
  pre-flag.** Per S3 notes: given Q1A's 0.18%-of-gap reading on the
  binding axis (hour_of_week, range 0.575), Q1B (time_of_day, range
  0.485) faces the same skepticism with weaker prior support. If
  Q1B fires R-B1B or R-C1B at similar magnitudes, the thread should
  declare time-indexed σ-algebra resolution exhausted and require an
  amendment for the temperature/exogenous-Z arm.

## Open questions

- **How aggressive should Q1B's Z2 construction be?** time_of_day has
  4 strata in P1's design (overnight / morning_ramp / afternoon /
  evening); the natural head-to-head is binary (overnight vs day)
  vs 4-level categorical. Q1A's empirical near-tie between Z_b and
  Z_c suggests the choice is unlikely to matter much, but the
  pre-registration should still be a proper head-to-head per the
  thread's methodology.

- **Should the EXHAUSTED-UNRESOLVED pre-flag trigger pre-emptive
  parallel work on temperature acquisition?** If Q1B is expected to
  exhaust the within-data lever, getting ECCC temperature data
  staged in parallel would compress the post-Q1B amendment + Q1C-
  reopening cycle. Low cost (data acquisition only; no new
  experiments before Q1B settles), high option value.

- **Is there an honest writeup framing where (S12) is a HEADLINE
  result rather than a confirmatory follow-up to P1?** The 0.18%-
  of-gap reading is methodologically the strongest negative result
  the audit workflow has produced — it isolates the within-data Z
  lever as empirically bounded with full pre-registration and exact
  reproduction-check. Worth considering as a §5 example of "what
  the audit workflow can prove a NEGATIVE about" alongside the
  positive S1/S2 findings.

- **Does Q1A' deferral risk losing the disintegration_diagnostic
  §(f) framework hypothesis test?** §(f) is "can data-driven methods
  recover hour_of_week blindly?" — independent of Q1A's verdict.
  With S12 in hand (within-data lever exhausted), the §(f) question
  is arguably MORE interesting now, not less. The deferral is about
  design quality of the gate, not about whether the question
  matters.

## Next session entry point

1. **Fill Q1B phase_a.yaml** via the `preregister` agent. Z2
   constructed from time_of_day (P1's 2nd-strongest axis, range
   0.485) per thread skeleton `Q1B.phase_a_skeleton`. Cumulative
   chi^2 computed against Q1A's Z_c-conditioned baseline (with the
   near-tie caveat — Z_c is parsimony-preferred point-estimate
   winner, not statistically discriminated from Z_b at 95%). Then
   `devils-advocate` writes the counter-prediction stamped
   simultaneously with the proponent.

2. **Q1A' FastICA design decision required (deferred from this
   session).** Three options on the table:
   - (1) amend selection rule from max-|excess_kurtosis| to
     max-|Spearman ρ vs Y| (the rule that composes with the verdict
     statistic; Hyvärinen & Oja Ch. 13 supports when target IS Y).
   - (2) drop FastICA from the head-to-head; Q1A' becomes SIR-only
     (cleaner: single carve-out instead of compounding amendments).
   - (3) extend Q1A' window with a different selection criterion
     (less convergent option; not recommended by advisor).

3. **ECCC temperature data acquisition** (background; given S12's
   exhausted-within-data-lever reading, increasingly on the
   critical-path for the resolution-paths-thread's post-Q1B
   trajectory). Not blocking Q1B build; pre-requisite for the
   temperature/exogenous-Z arm if Q1B fires R-B1B or R-C1B.

4. **Build → audit → run → arbiter for Q1B** following the Q1A
   pattern. Q1B's outcome triggers thread-coordinator dispatch
   per its registered branching_rules (R-A1B resolves; R-B1B / R-C1B
   route to P2 synthetic Gaussian-DGP calibration).

5. **Goal-4 paper update**: with S11 + S12 in hand, the resolution-
   paths arc has matured into a substantial thread with a
   publication-grade negative result on the within-data Z lever.
   Consider promoting it to a top-level section (or strengthening
   the appendix table) when next touching `writeup/tex/draft.tex`.

6. **Backfill S11 + S12 numerics into
   `writeup/tex/missing_content_memo.tex`** when next touched
   (memo predates both).
