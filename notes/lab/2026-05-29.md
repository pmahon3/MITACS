---
date: 2026-05-29
session_id: 2026-05-29-a
focus:
  - q1b-z2-conditioning-end-to-end
  - q1b-phase_a-pre-stamp-chain-amendment-p1-binning-correction
  - q1b-build-with-dual-anchor-reproduction-checks
  - q1b-settled-r-b1b-substantive-favorable-s13
  - thread-s4-q1b-settled-thread-paused-no-auto-advance
  - framework-reading-sharpened-not-refuted-ci-wide-caveat
commits:
  - 'e6a1189 CLAUDE.md (S12): soften EXHAUSTED-UNRESOLVED framing to advisory'
  - 'a606ef9 preregistration/q1b: phase_a + pre-stamp-chain amendment'
  - '40ac261 preregistration/q1b: proponent + DA stamped simultaneously'
  - '1736e71 experiment/q1b_z2_conditioning: build + Check P PASS'
  - 05ac800 Q1B SETTLED R-B1B (substantive FAVORABLE; CI-wide, framing-cautious)
preregistrations_touched:
  - 2026-05-27_q1b-z2-conditioning-second-axis
  - 2026-05-27_resolution-paths-thread
memory_files_touched:
  - MEMORY.md
  - mitacs-q1b-z2-conditioning.md
writeups_touched: []
cross_programme_refs:
  - ~/Research/Mathematics/Resolvent_Framework/notes/unsorted/disintegration_diagnostic.md (cited as framework-side identifiability source; not modified this session — refresh deferred)
settled_this_session:
  - 2026-05-27_q1b-z2-conditioning-second-axis
dispatched_this_session: []
deferred_to_next_session:
  - 'User decision LOCKED: option (d) — tightening run on Q1B BEFORE (a)/(b)/(c) adjudication. Next-session task: variant Q1B with N_PARTICLES=400 (vs current 200) to halve SMC noise contribution to the CI [+8.06, +484.4]; ~3 min compute. If CI narrows to ~[+50, +400], discriminates (a)/(b)/(c) cleanly. If CI stays ~[+8, +484], empirical evidence eval-window-noise dominates → accept wide CI, pick (a)/(b)/(c) on substantive grounds. The tightening run is itself a NEW pre-registration (Q1B'' — "Q1B-prime") because it modifies N_PARTICLES from the registered methodology. Phase_a goal: bracket whether the CI narrows under denser SMC.'
  - 'After tightening run: user adjudicates (a)/(b)/(c) on the (possibly narrower) CI. (a) third within-data Z axis [season 0.135 / demand_quantile 0.075; weaker prior than Z2 was]; (b) new thread targeting cross-axis joint stratification systematically; (c) temperature/exogenous-Z arm (Q1C reopen or new thread + ECCC data acquisition).'
  - Q1A' FastICA design decision STILL DEFERRED (from session 2026-05-28-a) — three options on the table per the earlier deferral; Q1A' is non-gating and parallel to the thread state machine, doesn't block
    the (a)/(b)/(c) decision.
  - 'CLAUDE.md (S13) entry pending — deliberately held off this session per advisor catch: the ''Settled findings'' section is load-bearing across writeups and future experiments; locking in the point estimate
    (40% closure, 218× Q1A) before CI tightens risks anchoring framework reading on a number that could be off by 60×. Ship S13 only after either (i) the (a)/(b)/(c) decision is made and a follow-up tightens
    the estimate, or (ii) user explicitly directs CLAUDE.md S13 write with current CI.'
  - Backfill (S11) + (S12) + (S13) numerics into writeup/tex/missing_content_memo.tex when next touched (memo predates all three)
  - ECCC temperature data acquisition (still on background list; if (c) is chosen, this is the pre-requisite for the temperature/exogenous-Z arm)
status_at_end: wrapped
---

# Session 2026-05-29-a — Q1B SETTLED R-B1B substantive FAVORABLE (S13), thread S4 → paused

## What happened

Continuation of session 2026-05-28-a's deferred items, picking up at
the natural next gating step: Q1B phase_a → audit → run → arbiter →
thread S4. End-to-end Q1B in one session, but with a substantive
surprise that the workflow caught cleanly through its CI-aware
discipline.

**Step A (Q1B phase_a + pre-stamp-chain amendment, commits a606ef9 +
a606ef9 amendment-in-same-file).** User pre-locked two design
decisions before dispatching the preregister agent: (i) head-to-head
binary vs 4-level on time_of_day (mirrors Q1A pattern); (ii) JOINT
stratification (day_type × Z_c × Z2). Ran empirical cell-count
feasibility on the pre-cutoff library + post-cutoff eval window
under what I *thought* was P1's binning (6/6/6/6 hours per stratum).
Dispatched preregister agent. **Agent caught a defect in my prompt**:
P1's production `_time_of_day_label` at
`experiment/p1_patra_sen/__main__.py:81-96` uses 5/5/6/7 ranges
(overnight h0∈{0..5}, morning_ramp h0∈{6..10}, afternoon h0∈{11..16},
evening h0∈{17..23}), NOT my 6/6/6/6. Agent stamped phase_a using
P1's source-of-truth binning.

Re-ran cell-count check under the correct binning. **One material
new fact**: post-cutoff cell (weekday, Z_c=5, morning_ramp) at n=144
(below MIN=200) on the eval side. Pre-cutoff still has all 32 joint
cells ≥ 2,364 (no fit-side fallback). The phase_a's
threshold_derivations paragraph reported feasibility numbers from
the wrong binning, so it was factually inconsistent with §metric.
**First pre-stamp-chain amendment** (precedent: P1 F_b switch
99a8122; Q1A' SIR cut 78b5069 — both pre-data first amendments):
added top-level `amendment_note` field; corrected the empirical
cell-count paragraph; documented the n=144 cell. Verdict cuts,
pool-fallback rule, and §metric (which was already using P1's
binning correctly per the agent's catch) UNCHANGED. Body sha256
ca231eb7… → **0602f0c0…**. `verify_file` PASS.

**Step B (proponent + DA stamped simultaneously, commit 40ac261).**
Both agents dispatched in parallel (workflow rule: non-observation,
not stamp ordering — session 2026-05-27-b process incident).
Forecast contrast — both used numeric point + CI per workflow
discipline:

```
                       Proponent (R-A1B, conf 0.40)   DA (R-B1B, conf 0.68)
better_chi2            180  [90, 450]                 948  [620, 1340]
chi2_diff              +160 [+20, +320]               -9   [-95, +75]
winner                 Z2_c                           Z2_b
cumulative impr        772.5  (107% gap)              4.5  (Q1A-comparable)
shape                  decisive Z2_c win              NEAR-TIE
```

Proponent argued P1's α̂_L gradient (overnight 0.11 vs daytime
0.48-0.60) is a structured 5× span the 4-level Z2_c isolates and
the binary Z2_b collapses. DA argued time_of_day is a coarser cut
than hour_of_week; Z_c's 8 21-hour bins already partition
within-day; only orthogonal signal is overnight-vs-rest captured
by Z2_b without granularity variance. **CIs barely overlap**
(proponent upper 450 vs DA lower 620) — the head-to-head was
genuinely uncertain pre-data.

**Step C (Q1B script build + audit chain, commit 1736e71).** Build
agent produced 1,764-line `experiment/q1b_z2_conditioning/__main__.py`
mirroring Q1A's pattern with four fit families (day_type parent,
(day_type, Z_c) parent, (day_type, Z_c, Z2_b), (day_type, Z_c,
Z2_c)) and TWO hard-asserted reproduction checks (vs Q2A 953.84 and
Q1A 952.5167). Inline synthetic gate with false-Z2 companion check.
CLAIM-GRADE provenance. `--gate-only` flag.

Audit chain:
- code-path: PRODUCTION-PATH verified (all `mixture_2_gaussian_mle_fit`
  + Q2A helpers are imports, no reimplementation).
- phase-fidelity Check P: **L1 18/18 PASS**, L2 8/9 PASS with one
  cosmetic advisory (the amendment_note's pool-fallback prediction
  about the n=144 post-cutoff cell is empirically interesting but
  §metric keys fallback on pre-cutoff library size where all cells
  ≥ 2,364; script correctly implements §metric).

**Step D (Q1B run + Check R + arbiter, commit 05ac800).** 78-second
run. **Reproduction checks BOTH EXACT** (strongest dual-anchor in
workflow history): Q2A 953.84 → |diff|=0.0033; Q1A 952.5167 →
|diff|=0.0000. Synthetic gate PASS in 0.1s including false-Z2
collapse. Pool-fallback ZERO events fired (Check P prediction
correct).

**Headline numerics:**
- chi2_Z2_b = 696.0 [492.3, 1009.0]
- chi2_Z2_c = 662.2 [479.8, 945.4]
- better_chi2 = 662.2 [468.1, 944.5] → **R-B1B** mechanical
- chi2_diff = +33.8 [-53.2, +124.5]; paired SE 44.71 → **NEAR-TIE**
- winner = Z2_c (at NEAR-TIE strength)
- **cumulative_chi2_improvement_above_Z_c = +290.3 [+8.06, +484.4]**

The point estimate is 40% closure / 218× Q1A's lever — surprising
given the joint S9+S10+S11+S12 "within-data bounded at 0.18%"
reading. But the CI [+8.06, +484.4] spans a factor of ~60×. The
lower bound is "barely above Q1A's 1.33"; the upper bound is "67%
closure within data."

Per-cell decomposition validates structural coherence: saturday__zc6
(Q1A's hot cell, chi^2=1634, P1 α̂_L=0.61) splits across Z2 as
morning_ramp 967 > evening 400 > afternoon 328. Sunday__zc7 similar
with morning_ramp dominating. The joint σ-algebra picks up real
structure that hour_of_week alone could not isolate.

**Methodologically interesting per-cell finding**: overnight (z20)
cells contribute substantially per-cell (sunday__zc6__z20=267,
saturday__zc5__z20=259, weekday__zc1..4__z20=174-211) DESPITE
P1's axis-level α̂_L=0.11 for overnight predicting "essentially
Gaussian." **Axis-level α̂_L does NOT cleanly predict cell-level
chi^2 contribution under joint stratification.** The proponent's
"P1 gradient explains per-cell ordering" story failed
*structurally* (not just on magnitude).

**Arbiter rendered SETTLED R-B1B substantive FAVORABLE.** Sixth
named-failure-mode instance in a new shape variant: mixed scoring
where outcome+shape match DA (R-B1B + NEAR-TIE), direction matches
proponent (Z2_c winner), and magnitude matches neither (realized 662
sits in the gap between proponent's CI upper 450 and DA's CI lower
620). Framework reading SHARPENED, not refuted: single-axis within-
data lever IS bounded near zero (Q1A); JOINT multi-axis is bounded
above zero with WIDE CI; full closure to R-A1B doesn't fire even at
upper CI (point 662 still 2.9× above cut 228).

**Step E (thread-coordinator S4 advance, commit 05ac800).** Q1B
active → settled; current_node_id Q1B → null (paused; no auto-
advance under branching_rules.Q1B.R-B1B=null). Thread does NOT
auto-resolve (40% < 80% closure_rule threshold) and does NOT auto-
exhaust. S3 EXHAUSTED-UNRESOLVED pre-flag DOWNGRADED with explicit
reason (premise refuted by 218×-Q1A realization AT POINT). Three-way
amendment decision space surfaced for user adjudication:
- (a) Third within-data Z axis (season range 0.135 or demand_quantile
  range 0.075 — weaker P1 support than Z2 was);
- (b) New thread targeting cross-axis joint stratification
  systematically;
- (c) Temperature/exogenous-Z arm (Q1C reopen or new thread per
  disintegration_diagnostic.md; ECCC data acquisition pre-requisite).

Coordinator did NOT pre-commit a direction. New thread body_sha256
173b3d58... → 91d529c9...; chain verified across all 13 registry
entries; resolution-paths-thread remains THREAD:active.

**Step F (memory + MEMORY.md, this session).** Created
`mitacs-q1b-z2-conditioning.md` with **CI-prominent framing**: the
description field leads with "CI [8, 484] spans 60× so true magnitude
is uncertain," NOT with the 40% point estimate. Per advisor catch:
the "Settled findings" line in MEMORY.md leads with the wide-CI
caveat. CLAUDE.md (S13) entry deliberately deferred to avoid
anchoring framework reading on a number that could be off by 60×.

## Decisions

- **Lock Q1B SETTLED R-B1B as substantive FAVORABLE (S13).** Joint
  multi-axis within-data conditioning recovers signal that single-axis
  could not — confirmed by reproduction checks, synthetic gate, and
  P1-coherent per-cell decomposition. Magnitude bounded above zero
  with high confidence; magnitude IS uncertain across 60× CI range.

- **Lock the framework-reading SHARPENING** (not refutation) of joint
  S9+S10+S11+S12: single-axis within-data lever bounded near zero
  (Q1A 0.18%); joint multi-axis within-data lever bounded above zero
  with wide CI; full closure to R-A1B remains unachieved even at
  upper CI of Q1B's CI. The identifiability obstruction picture at
  FULL-CLOSURE level is preserved as motivation.

- **Lock thread state at current_node=null with three-way amendment
  decision space surfaced.** The coordinator does not pre-commit;
  user adjudicates among (a) third within-data axis, (b) new
  cross-axis joint thread, (c) temperature arm. The right call
  depends on whether tightening Q1B's wide CI matters more than
  exploring the next axis.

- **Lock the 6th named-failure-mode instance.** Mixed scoring shape
  where outcome+shape from one forecaster, direction from the other,
  magnitude from neither. Realized falls in the gap between two
  pre-registered CIs.

- **Lock the methodological finding**: axis-level α̂_L does NOT
  predict cell-level chi^2 contribution under joint stratification.
  Worth its own paragraph in the per-cell P1 corroboration story.

- **Defer CLAUDE.md (S13) entry** per advisor catch — load-bearing
  framing section, don't anchor on point estimate when CI spans 60×.
  Ship after either tightening run or explicit user direction.

## Open questions

- **Which amendment direction (a)/(b)/(c)?** The decision is the
  user's; coordinator surfaced it; advisor input warranted before
  pre-committing. The CI [+8, +484] is the discriminating signal:
  if truth is at lower CI, (c) is still priority; if at upper CI,
  (b) is priority.

- **Would a tightening run on Q1B itself (e.g., longer post-cutoff
  window via accruing forward data, or larger pre-cutoff library
  via earlier IESO archives) be a pre-requisite to the amendment
  decision?** The wide CI is partly a 12,000-row eval-window thing.
  Cost: time + IESO archive integration; benefit: narrower CI →
  cleaner discrimination of (a)/(b)/(c).

- **Does the axis-level-α̂_L-vs-cell-level-chi^2 disconnect matter
  for P1's settled finding?** P1 is SETTLED on axis-level signal
  (binding axis = hour_of_week, range 0.575). The per-cell finding
  in Q1B shows the axis-level → per-cell mapping is non-trivial
  under joint stratification. This doesn't undo P1, but the writeup
  framing for Goal-4 needs to be careful about not claiming
  α̂_L localizes contamination cell-by-cell.

- **Is the "substantive FAVORABLE" label robust to the CI caveat?**
  The arbiter chose FAVORABLE because point + lower CI > Q1A reading;
  but if the reader interprets "FAVORABLE" as "joint multi-axis is
  the answer," they're over-reading the point. The memory and
  arbiter both flag this; the writeup framing will need to too.

## Next session entry point

1. **TIGHTENING RUN on Q1B with N_PARTICLES=400** before (a)/(b)/(c)
   adjudication. User decision LOCKED end-of-session 2026-05-29-a:
   the (a)/(b)/(c) value depends on which CI endpoint is closer to
   truth, so tighten CI first. Design: register a NEW pre-experiment
   ("Q1B'" or similar) since N_PARTICLES change deviates from Q1B's
   registered methodology — phase_a goal is to bracket whether CI
   narrows under denser SMC. Expected ~3 min compute. If CI narrows
   to ~[+50, +400], discriminates (a)/(b)/(c) cleanly. If CI stays
   ~[+8, +484], empirical evidence that eval-window-noise dominates
   → accept wide CI and pick (a)/(b)/(c) on substantive grounds.

   The design space considered (for next session's preregister
   dispatch):
   - N_PARTICLES 200→400 (halves SMC noise; cheap; primary lever)
   - Larger pre-cutoff library via 2021-22 IESO archive
     (mitacs-ieso-data-sources; ~9% more rows; secondary)
   - Hessian-based asymptotic CI on per-cell mixture parameters
     propagated through SMC (methodologically novel; needs its
     own preregistration; defer unless N_PARTICLES variant
     shows SMC noise IS the dominant component)
   - Accruing forward post-cutoff data (4× rows would halve CI;
     ~5.5 years of forward accrual; not actionable this year)

2. **After tightening run, user adjudication of (a)/(b)/(c)**:
   (a) third within-data Z axis vs (b) new cross-axis joint thread
   vs (c) temperature/exogenous-Z arm. CI from the tightening run
   is the discriminating signal.

2. **Q1A' FastICA design decision still deferred** (from
   2026-05-28-a); non-gating, parallel to thread state machine,
   doesn't block (a)/(b)/(c). Three options on the table per the
   earlier deferral.

3. **ECCC temperature data acquisition** if (c) is chosen.
   Pre-requisite for the temperature arm; still on background list.

4. **CLAUDE.md (S13) entry write-up** — deferred this session per
   advisor catch. Ship after either a tightening run on Q1B or
   explicit user direction.

5. **Backfill (S11) + (S12) + (S13) numerics into
   `writeup/tex/missing_content_memo.tex`** when next touched
   (memo predates all three).

6. **Goal-4 paper update**: with Q1B's substantive FAVORABLE finding,
   the resolution-paths arc has matured from "negative result on
   within-data Z lever" to "joint multi-axis recovers wide-CI signal
   that single-axis could not — full closure still requires
   auxiliary information." Section §5 of `writeup/tex/draft.tex`
   can now carry a richer reading; defer until (a)/(b)/(c) is
   adjudicated to avoid writing it twice.
