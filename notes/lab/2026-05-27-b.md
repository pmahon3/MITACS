---
date: 2026-05-27
session_id: 2026-05-27-b
focus:
  - resolution-paths-thread-active
  - p1-patra-sen-built-run-settled
  - thread-coordinator-s2-advancement
  - q1a-z-conditioning-preregistered
  - q1a-prime-sdr-ica-sibling-preregistered-amendment-A1
commits:
  - 99a8122 P1 methodology amendment — F_b=N(0,1) on u_std → F_b=Uniform[0,1] on u_PIT (pre-run carve-out)
  - 3c3e2e0 experiment/p1_patra_sen — build P1 script, code-path PRODUCTION-PATH + Check P L1+L2 PASS
  - 9d89a29 P1 SETTLED R-A (S11) — hour_of_week binds the κ_1 PIT residual structure
  - 1775873 thread-coordinator — resolution-paths-thread S2 (P1 settled → Q1A active)
  - 7f8cb28 session-bookkeeping — CLAUDE.md (S11) + lab note 2026-05-27-b (initial write)
  - 769d854 experiment/q1a_z_conditioning — preregister Q1A (phase_a + proponent + DA; head-to-head Z_b/Z_c)
  - 3748819 experiment/q1a_prime_sdr_ica — preregister Q1A' sibling + thread amendment A1 (non-gating SDR/ICA consistency check)
preregistrations_touched:
  - 2026-05-27_p1-patra-sen-per-stratum-localization
  - 2026-05-27_resolution-paths-thread (amendment A1 — added Q1A' as non-gating sibling)
  - 2026-05-27_q1a-z-conditioning-strongest-axis (created; phase_a + proponent + DA)
  - 2026-05-27_q1a-prime-sdr-ica-sibling (created; phase_a + proponent + DA)
memory_files_touched:
  - mitacs-p1-patra-sen-localization.md (created in 2026-05-27-a; referenced by Q1A/Q1A' preregisters)
  - mitacs-clim-gap-nonstationary.md (STATUS AMENDMENT added in 2026-05-27-a)
  - MEMORY.md (already has (S11) under Settled findings from 2026-05-27-a)
writeups_touched: []
cross_programme_refs:
  - ~/Research/Mathematics/Resolvent_Framework/notes/unsorted/disintegration_diagnostic.md (full read; §(f) cited as methodological grounding for Q1A' amendment)
settled_this_session:
  - 2026-05-27_p1-patra-sen-per-stratum-localization
dispatched_this_session:
  - 2026-05-27_q1a-z-conditioning-strongest-axis (phase_a body_sha256 a2b13f6d...; proponent 5a6dc421...; DA 8d8d4b38...)
  - 2026-05-27_q1a-prime-sdr-ica-sibling (phase_a body_sha256 b14d09cb...; proponent 92e5b8f2...; DA 483477a5...)
  - 2026-05-27_resolution-paths-thread amendment A1 (post-amendment body_sha256 21a8bb20...; prior_tree_hash 9a5647a5...)
deferred_to_next_session:
  - "Build experiment/q1a_z_conditioning/__main__.py — production-path mixture_2_gaussian_mle_fit per (day_type, Z) cell head-to-head Z_b/Z_c; inline synthetic gate; CLAIM-GRADE provenance via make_result()"
  - "Build sliced_inverse_regression_fit production helper in processing/innovations/estimator.py + whitelist update in experiment/audit/code_path.py + synthetic recovery gate per processing/innovations/validation/synthetic.py (pattern: patra_sen_fit from P1)"
  - "Build experiment/q1a_prime_sdr_ica/__main__.py — production-path SIR + sklearn FastICA head-to-head; inline synthetic recovery gate at SNR ≈ 1.5 (BLOCKING, ≥ 0.70 on synthetic, < 0.30 on permuted null); CLAIM-GRADE provenance"
  - "/audit code-path on both scripts (BLOCKING); /audit phase-fidelity Check P on both (L1 BLOCKING, L2 advisory)"
  - "Run both experiments (independent; either order). Q1A is the gating arbiter for the thread state machine; Q1A' is the independent consistency check"
  - "Check R + arbiter for both. Q1A's arbiter triggers thread-coordinator dispatch (R-A1A resolves the thread; R-B1A branches to Q1B; R-C1A branches to P2). Q1A' arbiter does NOT trigger thread-coordinator (non-gating)"
  - "Backfill (S11) numerics into writeup/tex/missing_content_memo.tex when next touched (memo predates P1)"
  - "ECCC temperature data acquisition — still relevant for the marginal pathology even after S11; not on Q1A's critical path but pre-requisite for Q1B if Q1A.R-B1A fires"
status_at_end: wrapped
---

# Session 2026-05-27-b — P1 SETTLED R-A, resolution-paths-thread at Q1A

## What happened

Continuation of session 2026-05-27-a (Q2A/Q2B settled, distributional-
class-thread closed, lab notebook MVP, P1 preregistered). This block
walked the resolution-paths-thread through its first node end-to-end:
P1 build → run → arbiter, ending with thread-coordinator advancing the
state machine to Q1A.

**Step A (thread-coordinator: planning → active).** The
`2026-05-27_resolution-paths-thread` was advanced from `planning` to
`active` with `current_node_id = P1`. P1's `phase_a` + `proponent` +
`devils_advocate` had been stamped at the tail of 2026-05-27-a; what
remained was the state transition record with `prior_hash` written into
`state_history` so the descendant arbiter could back-reference the
thread artifact.

**Step B (production function + recovery gate).** Built `patra_sen_fit`
in `processing/innovations/estimator.py` — the Patra-Sen 2016
two-component mixture estimator α̂_L (lower 95% bound on the
non-Gaussian mixture fraction). Added it to the code-path auditor
whitelist in `experiment/audit/code_path.py`, and stamped a synthetic
recovery gate at α_true ∈ {0.0, 0.5} for both F_b='gaussian' and
F_b='uniform' under `processing/innovations/validation/synthetic.py`.
Gate cleared. This is the workflow's standard pattern: no Ontario data
touched until the production function passes the recovery gate.

**Step C-prep (lab notebook 1:1 coverage).** Back-filled `note.md` for
all eleven registry entries (commit 90cfc2a, technically the tail of
session 2026-05-27-a but listed here for narrative continuity). The
`arbiter` and `thread-coordinator` agent definitions both now write
note.md as part of their procedures — one-shot 1:1 enforcement going
forward. This is the load-bearing dashboard change: notes render
inline above the YAML accordion on every registry entry in the
lab-notebook Home tab, with MathJax + slug cross-links.

**Step C-amend (methodology carve-out, pre-run).** During the build,
caught a defect in P1's `phase_a.yaml` before any Ontario data was
read: the proponent had registered F_b = N(0,1) on standardized
residuals u_std. The correct null under Patra-Sen Theorem 1's
strictly-monotone reparameterization is F_b = Uniform[0,1] on the PIT
residuals u_PIT — every PIT transform is by construction Uniform under
its own model. Re-stamped phase_a with the corrected null per the
pre-run methodology amendment carve-out (commit 99a8122). The
proponent's R-A forecast and the DA's R-C forecast translate
unambiguously through Theorem 1's monotone invariance and were not
re-stamped; only the calibrated null changed. Both forecasters' point
estimates and CIs were locked under the original null and remain
locked. The carve-out is the workflow's intended response for "defect
caught before data is touched"; it is the second invocation in the
workflow's history (the first was the eigenmodes-v1 multiverse
specification at 2026-05-25).

**Step C-build (P1 experiment script).** Built
`experiment/p1_patra_sen/__main__.py`. Inline synthetic gate
(BLOCKING) before any Ontario read — runs the production
`patra_sen_fit` against α_true ∈ {0.0, 0.5} once more from the script
itself; if recovery exceeds tolerance the script aborts before
touching IESO data. Production-path audit (`/audit code-path`) PASS;
phase-fidelity Check P L1 (structural) + L2 (methodological) PASS
(commit 3c3e2e0). Emits a CLAIM-GRADE provenanced result.txt via
`experiment.provenance.make_result()`.

**Step C-run.** Full 1000-rep paired-day bootstrap, 217s wall-clock on
the dev box. CLAIM-GRADE provenanced artifact at
`experiment/results/p1_patra_sen.txt` (body-hashed; `frozen_spec_hash
f0dd9c87...`; `inputs_fingerprint b7d64663...`).

**Step C-tail (Check R, arbiter, memory update, thread S2).**
phase-fidelity Check R confirmed the result.yaml reports the
dependent variables phase_a registered (PASS). The arbiter rendered
SETTLED R-A and wrote `note.md` as part of its dispatch (the new
arbiter procedure step 7 from the agent-definition update). The
memory corpus picked up two artifacts: `mitacs-p1-patra-sen-
localization.md` (new) and a STATUS AMENDMENT block on
`mitacs-clim-gap-nonstationary.md` (the temperature-prior downward
update). Thread-coordinator advanced the resolution-paths-thread:
P1.status active → settled; Q1A.status planning → active;
Q1C.status planning → closed (sibling not fired); current_node_id
P1 → Q1A. `state_history` carries the prior thread.yaml's
body_sha256 for the back-reference (commit 1775873).

## Decisions

- **Lock P1 SETTLED R-A as the first applied success of the
  resolution-paths-thread.** R-A at landslide strength
  (`max_axis_range = 0.575` with lower CI bound 0.4475, more than 2×
  the R-A cut at 0.20). Three of five per-axis CIs entirely above
  R-A at their lower bound. The DA's R-C is decisively falsified.
  This is not a borderline verdict; the registered cuts catch a real
  signal at margin.

- **Lock the substantive headline: missing-Z is cyclical / social-
  weekly, not seasonal / climatological.** Binding axis is
  `hour_of_week` at range 0.575; `season` is fourth-of-five at range
  0.135. The proponent's temperature-via-season prior is 95%-CI
  falsified (season range CI upper bound 0.240 vs. the predicted
  0.30). Hot zone: bins 5/6/7 (Fri 10:00 - Sun 23:00, α̂_L 0.55-
  0.61); cold zone bin 0 (Mon 01:00-21:00, α̂_L 0.035). The
  `day_type` axis at coarser resolution shows the same ordering
  (saturday 0.6475 > sunday 0.485 > weekday 0.3025), independently
  corroborating Q2A Finding 2 and eigenmodes-v1 from a distinct
  estimator (α̂_L vs. χ²).

- **Pin Q1A's Z to hour_of_week per the P1 arbiter's binding-axis
  routing.** Per phase_a.shape_outcomes.R-A.interpretation, R-A
  routes to Q1A with the binding axis as Q1A's conditioning
  variable. The arbiter's notes_for_thread_coordinator pre-flags
  three Z constructions: (i) binary indicator of bin ≥ 5 (weekend
  window), (ii) 8-level categorical (preserves hour_of_week
  granularity), (iii) continuous hour-of-week-mod-168 phase
  variable. The phase_a author (next session) picks one or
  registers a head-to-head; this session does NOT pre-commit.

- **Downward update on the temperature-prior hypothesis, but
  temperature is not extinguished.** `clim-gap-nonstationary` is no
  longer the prior-supported localizing-axis candidate (S11
  empirically rejects season as the binding axis at 95% CI), but
  the +1000 MW post-cutoff temperature gap remains the strongest
  candidate for the marginal pathology. The STATUS AMENDMENT on
  `mitacs-clim-gap-nonstationary.md` records this distinction.
  If Q1A's hour_of_week Z fires R-B1A or R-C1A, temperature
  re-enters at Q1B (Q1C closed as sibling-not-fired, but Q1B
  remains a live downstream node).

- **Document the fourth named-failure-mode instance in a new
  orthogonal shape.** Mechanical verdict matches one forecaster's
  outcome category (proponent's R-A); within-bucket forecasts
  (binding axis, magnitude, marginal anchor) match neither side.
  This differs from Q2B's variant (substantive STRONGER than
  mechanical) and from eigenmodes-v1's variant (O2-vs-O3 near-tie
  on a different categorical axis). The mechanical-verdict-plus-
  substantive-content arbiter shape absorbs all three variants
  without engineering changes; per CLAUDE.md's applied-audit
  workflow section, this is the fourth named failure mode working
  as designed.

## Open questions

- **Which of the three Z constructions does Q1A's phase_a register?**
  (i) binary weekend indicator is parsimonious but discards the
  bin-5-vs-bin-6 contrast (hot zone is not monolithic — Saturday
  daytime α̂_L 0.61 is the heaviest stratum); (ii) 8-level
  categorical preserves the within-zone gradient but inflates the
  M3 parameter count by a factor of 8; (iii) continuous mod-168
  phase is the smoothest but assumes a sinusoidal-like dependence
  the data has not yet justified. A head-to-head between (i) and
  (ii) is the natural pre-registration if energy permits;
  otherwise (ii) is the default since it carries the most signal
  without functional-form assumptions.

- **Will R-A1A fire on Q1A?** The cut is marginal PIT χ² ≤ 228
  (Q2A's R-A2 gate-derived cut, the > 80% cumulative-explained-share
  threshold for resolution). Q2A's M3 baseline χ² ≈ 954; the per-
  stratum hot-zone α̂_L of 0.61 means roughly 61% of the heaviest
  stratum is non-Gaussian. Whether explicit conditioning on
  hour_of_week collapses the residual structure enough to clear
  228 is the empirical question. If R-A1A fires, the thread
  RESOLVES; if R-B1A, branch to Q1B (temperature); if R-C1A,
  branch to P2 (synthetic Gaussian-DGP calibration as the
  unresolved-thread reference baseline).

- **Does the (S11) finding warrant a new section in writeup/tex/
  draft.tex, or just an appendix-table row?** With the
  resolution-paths-thread arc now a substantial fourth thread
  (alongside multiscale, eigenmodes, distributional-class),
  the writeup's pre-registration narrative could either
  highlight the localize-first-identify-next pattern as a
  framework-led methodological contribution or fold it into
  §4.5's forward-experiment-only scope. The §4.5 scope is
  currently locked; revisiting needs a writeup-state decision.

## Next session entry point

1. **Fill Q1A's phase_a.yaml** via the `preregister` agent. The
   arbiter's `notes_for_thread_coordinator` on
   `notes/preregistrations/2026-05-27_p1-patra-sen-per-stratum-
   localization/arbiter.yaml` pins hour_of_week as Z and lists the
   three pre-flagged Z constructions. Then `devils-advocate` writes
   the counter-prediction stamped simultaneously with the proponent.

2. **`/audit code-path` and `/audit phase-fidelity` Check P** on
   the Q1A script. Q1A re-fits M3 (mixture-2-Gaussian, the Q2A
   baseline) with Z added; the production-path question is whether
   M3-with-Z can be built by extending Q2A's M3 path or whether
   it needs a new estimator.

3. **Run Q1A**, Check R, arbiter. R-A1A RESOLVES the thread
   (write-up moment); R-B1A branches to Q1B (temperature); R-C1A
   branches to P2 (synthetic calibration).

4. **ECCC temperature data acquisition** as parallel infrastructure
   work. Not on Q1A's critical path, but pre-requisite for Q1B if
   the branch fires. Worth a fetch-and-stash session if energy
   permits while Q1A runs.

5. **(c) follow-up if useful:** the kernel-family-agnostic theory
   memo predates (S11); the memo is not a publication target so
   refreshing it is low-urgency, but if the `/audit pure` audit
   on the theory side gets dispatched, fold (S11) in at the same
   time.

---

## Continuation: Q1A + Q1A' preregister (post-2026-05-27-b initial write)

The "Next session entry point" above turned out to be over-pessimistic
on session budget. The same calendar afternoon continued through:
(c) CLAUDE.md (S11) update, (a) the initial 2026-05-27-b lab note
(this file's first 250 lines), (b) Q1A preregister + forecasts, and
— after a cross-programme review checkpoint — the addition of Q1A'
as a non-gating SDR/ICA sibling under P1.R-A (thread amendment A1).
This section is the append-only continuation.

### Q1A preregister (commit 769d854)

**phase_a**. Dispatched the `preregister` agent against the
resolution-paths-thread's Q1A skeleton. The skeleton (locked by
thread pre-registration) fixes the metric_class (re-fit M3 with
hour_of_week-derived Z), the cuts (228 / 2284 / >2284 per Q2A's
gate-derived corroboration cut), and the branching (R-A1A → null
resolves the thread, R-B1A → Q1B, R-C1A → P2). The Z construction
was the only open design choice. User direction: register a
**head-to-head between (i) binary weekend indicator** and **(ii)
8-level categorical** preserving hour_of_week granularity; (iii)
continuous mod-168 phase out of scope.

The agent landed a clean phase_a (body_sha256 `a2b13f6d...`,
+543 lines including the proponent and DA stamps that followed):
verdict rule `min(chi2_Z_b, chi2_Z_c)` against the locked cuts;
pre-locked tie band at `|chi2_Z_b − chi2_Z_c| < 1.0 σ_paired_bootstrap`
with sub-finding "no ranking" when in band; landslide qualifier on
R-A1A corroboration (both point and lower 95% CI bound clear 228);
production-path commitment to `mixture_2_gaussian_mle_fit` (the
existing Q2A predictor — no reimplementation); MIN_CELL_SIZE = 200
pool-to-parent fallback for sparse cells (the smallest cell is
sunday bin 6 at n=213).

**Forecasts (stamped simultaneously, neither saw the other).**
Proponent R-A1A1 [chi2_min 200, CI [130, 320], Z_c wins by +220,
landslide false, confidence 0.55]. DA R-B1A [chi2_min 800, CI
[540, 1080], Z_b wins by −150, confidence 0.55]. Critically the
CIs **DO NOT overlap** (proponent upper 320 vs DA lower 540) — a
cleaner separation than P1 had. The forecasts disagree on outcome
category AND on which Z construction wins; the verdict will
discriminate.

### Cross-programme review checkpoint: disintegration_diagnostic.md

Before continuing to the build phase, user requested a full read of
the theory-side `disintegration_diagnostic.md` — the framework note
S10's identifiability-obstruction reframe was built on. Key findings
from the read:

- **§(e) explicitly lists Q1A as the natural next experiment** in
  the resolution-paths sequence. No retraction or amendment needed
  on Q1A's design.
- **§(f) "Data-driven Z-recovery tools (SDR, ICA, causal discovery)"**
  recommends running these *in parallel* with Q1A as a consistency
  check: "if these methods recover hour_of_week blindly, that
  validates the approach for future Z-discovery when the candidate
  set is less obvious." This was not on the thread skeleton.
- **§(a) irreducibility caveat**: P1's α̂_L is a lower bound on the
  maximum identifiable proportion, not an estimate of the true α₀.
  Doesn't change the Q1A verdict (cuts are on χ², not α̂), but
  qualifies the proponent's rationale if R-B1A1 fires marginally.

The §(f) recommendation surfaced a design question I had not
anticipated: should SDR/ICA be added as a sibling to Q1A? User
direction (after a question-set walking through the tradeoffs):
**yes, as a sibling under P1, runs in parallel to Q1A, non-gating
independent verdict, head-to-head between SIR + FastICA.**

### Q1A' addition: thread amendment A1 (commit 3748819)

**The schema-and-policy obstruction caught and resolved**. First
pass at this design ran into the settled-node immutability rule:
"Amendments cannot change `branching_rules[node_id]` for nodes
that have already settled." P1 settled with `branching_rules.P1.R-A
= Q1A`; expanding that to `[Q1A, Q1A']` would change the rule on
a fired branch. Advisor pointed at the schema-clean path I'd
missed: **add Q1A' as a new node with `parent_branch: R-A,
parent_id: P1, status: active`, and do NOT touch
`branching_rules.P1.R-A` at all.** The parent metadata is
descriptive; the branching rule is execution. Q1B already uses
the same pattern (parented under P1.R-A in metadata, reached
execution-wise via Q1A.R-B1A in branching_rules). No rule against
multiple `active` nodes simultaneously; `current_node_id` stays
`Q1A` (the gating node for state-machine progression); Q1A' lives
out-of-state-machine and settles independently.

Thread-coordinator amendment A1 applied: 6 nodes now (P1 settled;
Q1A and Q1A' both active; Q1B, P2 planned; Q1C closed).
prior_tree_hash `9a5647a5...`; post-amendment body_sha256
`21a8bb20...`. Chain verifies.

### Q1A' preregister (commit 3748819, same batch)

**phase_a**. The agent (29.9 kB, body_sha256 `b14d09cb...`)
locked: SIR + FastICA head-to-head; input covariate matrix
explicitly *excludes* hour_of_week (the discriminating test
is whether methods recover it WITHOUT pre-specification);
includes demand_quantile, time_of_day, season, day_type +
lag-1, lag-24 demand; cyclic hour_of_week reference computed
at the weekly fundamental sin/cos at 2π/168; verdict statistic
`max(|ρ_SIR|, |ρ_FastICA|)` against the thread-skeleton cuts
0.5 / 0.3 (R-A1A' / R-B1A' / R-C1A'); permutation-null
baseline as informational chance-level anchor; METHOD/DESIGN
synthetic recovery gate BLOCKING (require max(|ρ_synth|) ≥ 0.70
AND max(|ρ_synth_null|) < 0.30 before Ontario run); landslide
qualifier on R-A1A' (both point and lower CI ≥ 0.5);
independence clause explicit; production-path commitment to a
new `sliced_inverse_regression_fit` helper to be built in
`processing/innovations/estimator.py` (mirroring `patra_sen_fit`
from P1) + sklearn's FastICA.

**Forecasts**. Proponent R-A1A' [rho_max 0.60 CI [0.50, 0.71],
FastICA wins by +0.15, landslide TRUE-but-marginal (lower CI
exactly at the cut), confidence 0.70]. DA R-B1A' [rho_max 0.42
CI [0.30, 0.54], SIR wins by −0.10, landslide false, confidence
0.50]. Genuine disagreement on outcome category, winner, AND
landslide qualifier.

### Process incident: Q1A' proponent socket failure + DA-first stamping

Both Q1A' forecasters were dispatched in parallel per the
workflow's simultaneity rule. The proponent agent hit a socket
error after ~14 minutes of work and zero file writes; the DA had
already stamped successfully. State on disk: phase_a + DA, no
proponent.

Advisor clarification on the workflow integrity question: the
simultaneity rule is preserved by **non-observation**, not by
stamp ordering. The original proponent was a separate process
with no memory of the DA dispatch; the re-dispatched proponent
is also a separate process. Whether the DA file exists on disk
is observable only via a read the prompt forbids. As long as
the agent doesn't read the DA file, the proponent's forecast is
no less independent than if the DA had stamped two seconds later
instead of two seconds earlier.

Re-dispatched the proponent with explicit hard-prohibition on
reading the DA file (added the literal path + glob patterns to
the prompt's "do not read" list; added a `process_note` field
on the proponent.yaml documenting compliance). Proponent
confirmed via tool-history self-check that no Read, Bash cat/
head/grep, or any other inspection touched any `devils_advocate.yaml`
file in any directory. Documented as an inline addition to the
session lab note rather than a formal amendment — nothing about
the registered methodology changed.

### Decisions (continuation)

- **Lock Q1A as the gating node and Q1A' as the independent
  consistency check.** Q1A's outcome routes thread state per the
  pre-registered branching_rules; Q1A''s outcome is recorded
  independently. Both will settle this calendar week. The §(f)
  framework hypothesis — that data-driven methods should recover
  P1's hand-specified axis blindly — is now a numeric pre-
  registered claim, not a free-floating expectation.

- **Lock the §(f) covariate-matrix exclusion rule.** Including
  hour_of_week in X would trivialize the test. The exclusion is
  the discriminating element of Q1A' as a *consistency check*;
  any future SDR/ICA experiment under this thread that re-includes
  hour_of_week is a different question.

- **Lock the workflow integrity reading of the simultaneity rule.**
  Non-observation, not ordering. Documented in this session lab
  note's process incident section. Future occurrences (the
  agent-dispatch protocol is intrinsically race-y) follow the
  same response: re-dispatch with explicit do-not-read instruction
  on the already-stamped file; add a `process_note` field to the
  recovering artifact.

### Open questions (continuation)

- **Will Q1A and Q1A' fire concordantly?** A natural sub-finding
  to look for at arbiter time: if Q1A R-A1A fires (χ² ≤ 228) AND
  Q1A' R-A1A' fires (max(|ρ|) ≥ 0.5), the joint reading is
  strong corroboration — both the χ²-closure and the data-driven
  recovery converge. If they fire discordantly (e.g., Q1A R-A1A
  but Q1A' R-C1A'), the substantive interpretation gets
  interesting: "hour_of_week conditioning closes χ² but SDR/ICA
  doesn't recover hour_of_week" would imply the χ² closure is
  through a non-cyclic representation (the 8-bin categorical
  may absorb the variance through bin-specific mean/variance
  shifts rather than through the cyclic phase per se).

- **Is the lab note workflow under-automated?** The session
  frontmatter (`commits`, `dispatched_this_session`,
  `deferred_to_next_session`) was manually edited at session
  wrap; no agent owns the lab note. Whether this should be
  automated (a `session-coordinator` agent or a stop-hook) is
  a workflow-design question raised at the tail of this session.
  Deferred — listed as an open issue in the next-session
  entry-point bullets.

### Next session entry point (continuation, supersedes the
above original)

1. **Build `experiment/q1a_z_conditioning/__main__.py`** —
   production-path Q1A script following the P1 pattern. Calls
   `mixture_2_gaussian_mle_fit` per (day_type, Z) cell for both
   Z_b and Z_c head-to-head; emits CLAIM-GRADE provenanced
   result.txt via `make_result()`; inline synthetic gate
   (BLOCKING) before any Ontario read.

2. **Build `sliced_inverse_regression_fit` in
   `processing/innovations/estimator.py`** + whitelist in
   `experiment/audit/code_path.py` + synthetic recovery gate in
   `processing/innovations/validation/synthetic.py`. Pattern:
   `patra_sen_fit` from P1 last session.

3. **Build `experiment/q1a_prime_sdr_ica/__main__.py`** —
   production-path SIR + sklearn FastICA head-to-head; inline
   synthetic recovery gate at SNR ≈ 1.5 (BLOCKING).

4. **Audits on both scripts** — `/audit code-path` (BLOCKING)
   then `/audit phase-fidelity` Check P (L1 BLOCKING).

5. **Run both experiments** (independent; either order; Q1A is
   gating for thread state machine, Q1A' is independent).

6. **Check R + arbiter for both.** Q1A's arbiter triggers
   thread-coordinator (S3) per outcome; Q1A''s arbiter does
   NOT trigger thread-coordinator (non-gating).

7. **Decide on lab-note workflow automation** — workflow gap
   surfaced this session. Either accept manual editing as the
   load-bearing-discipline default (analogous to MEMORY.md
   updates) or sketch a `session-coordinator` agent. Low-
   priority; not blocking science.
