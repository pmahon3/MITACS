## resolution-paths-thread

### The question

This thread asks which of the three framework-prescribed resolution paths closes the residual $\chi^2 \approx 954$ of the $\kappa_1$ predictor on post-cutoff Ontario data, where "closure" is defined as cumulative explained share $> 0.80$ against the Q2A baseline. The thread's scope is narrow by design: it tests the $\sigma$-algebra-enrichment axis with auxiliary information $Z$, and it does **not** re-test the distributional-class axis — that lever was bounded by [2026-05-26_q2a-richer-family] (S9) and [2026-05-26_q2b-non-distributional-decomposition] (S10). The framework grounding is the identifiability obstruction parked at `~/Research/Mathematics/Resolvent_Framework/notes/unsorted/disintegration_diagnostic.md` (with its `disintegration_diagnostic_audit_correction.md` companion): per Bergna et al. (2026, Prop 1), Heckman & Singer (1984), and Allahverdyan (2020), structured-noise and noisy-structure are observationally indistinguishable from $\kappa_Q$ alone. The joint S9+S10 verdict is the applied confirmation of that obstruction; resolution requires breaking the tie via information outside $\kappa_Q$, i.e. enriching the conditioning $\sigma$-algebra $\mathcal{Q}$ to $\mathcal{Q}' = \mathcal{Q} \vee \sigma(Z)$. All results are attribution-grade — adding $Z$ to a predictor changes its conditioning $\sigma$-algebra, so this thread does not yield deployable forecasts but rather a Bayes-risk decomposition of the residual non-Gaussianity. Amendment A4 (2026-05-28) grafts a temperature-arm sub-tree onto the thread to continue the same research question with $Z$ drawn from outside the time index — extending path (i) candidate-$Z$ testing to exogenous $Z$.

### The tree and the rules

The original tree had five gating nodes plus P2 backstop; **amendments A1–A4** have grown it to ten. **A1 (2026-05-27)** added a sixth, **Q1A'**, as a non-gating sibling of Q1A under P1.R-A. **A2 (2026-05-29)** added a seventh, **Q1B'**, as a non-gating sibling of Q1B under R-A — an empirical CI-tightening run at `N_PARTICLES=400` (vs Q1B's $200$). **A3 (2026-05-29)** closed Q1A' as EXHAUSTED-PRE-DATA via thread amendment without running (FastICA selection-rule design defect). **A4 (2026-05-28)** grafts three new planned nodes — **T1**, **T2**, **T3** — onto a new `temperature_arm` branch, extending candidate-$Z$ testing to exogenous $Z$ beyond the time index per the kernel framing $\mathcal{Q}' = \mathcal{Q} \vee \sigma(Z)$ with $Z = $ temperature. **P1** (Patra–Sen per stratum) ran first as a localization diagnostic and SETTLED R-A (landslide) at S2: `max_axis_range` $= 0.575$ $[0.448, 0.648]$, binding axis = `hour_of_week`. **Q1A** SETTLED R-B1A at S3 (`better_chi2 = 952.5167`; closed only $0.18\%$ of the gap). **Q1B** SETTLED R-B1B with `_SUBSTANTIVE_FAVORABLE` suffix at S4 (`better_chi2 = 662.2233 [468.094, 944.452]`; cumulative reduction above Q1A's Z_c carryforward = $290.29$ $\chi^2$ $= 40.07\%$ of remaining gap). **Q1B'** SETTLED R-B1B' at S5 (CI width $506.198$ $\chi^2$ in registered $(357.27, 524.0]$ band; +6.3% WIDENING vs Q1B's 476.358, opposite both forecasters' narrowing predictions — confirms Q1B's wide CI is intrinsic to data, not estimator). **Q1A'** was exhausted pre-data via A3. **Q1C** is closed (sibling-not-fired at S2). **T1** is the new root of the temperature arm: re-fits the production mixture-2-Gaussian per `(day_type, Z_c, T1_temperature_Z)` cell using ECCC hourly weather as $Z_2$; cumulative baseline = Q1A's settled $952.5167$; outcome cuts inherit the thread skeleton's $(228, 2284]$ band as a starting point but may be recalibrated at T1 phase_a fill time. **T2** is a Q1B-analogue joint refinement fired only on T1.R-B1T (`branching_rules.T1.R-B1T = T2`). **T3** is a Q1B'-analogue methodological-tightening non-gating sibling on the temperature arm, pre-registered only if T1/T2 CI is wide; `branching_rules.T3` all-null per non-gating-sibling design. **P2** (synthetic Gaussian-DGP calibration) remains reachable as a backstop. The closure rule is unchanged: $$\text{share} = \frac{\chi^2_{\text{base}} - \chi^2_{\text{after}}}{\chi^2_{\text{base}} - \chi^2_{\text{R-A2 cut}}},$$ with cumulative share $> 0.80$ across tested $Z$'s $\Rightarrow$ RESOLVED, otherwise $\Rightarrow$ EXHAUSTED-UNRESOLVED. The NPMLEmix methods-novelty branch (Deb, Saha, Guntuboyina & Sen 2022) remains the canonical covariate-dependent generalization but is not pursued here — A4's `T1.R-C1T` routes to that path as a handoff target.

### Current position (post-A4; A4 written 2026-05-28)

**Amendment A4 grafts a temperature-arm sub-tree (T1, T2, T3) onto the resolution-paths-thread.** The within-data candidate-$Z$ arc has settled: P1 R-A landslide (S2), Q1A R-B1A (S3), Q1B R-B1B substantive favorable (S4), Q1B' R-B1B' aligned (S5); Q1A' exhausted pre-data via A3; Q1C closed at S2 as sibling-not-fired. The thread's research question is partially answered: path (i) candidate-$Z$ testing, restricted to the time-indexed $\sigma$-algebra, is empirically bounded — single-axis bounded near zero (S12), joint multi-axis bounded at higher magnitude (S13) but not closing to R-A1B, and methodological tightening empirically bounded (S5). The joint S9+S10+S11+S12+S13+S5(Q1B') record is publication-grade evidence for the identifiability obstruction under time-indexed $\sigma$-algebras alone. **A4 extends candidate-$Z$ testing to exogenous $Z$ beyond the time index** — the same framework-prescribed resolution path (i) the thread was opened to test, with the $\sigma$-algebra restriction lifted. Per the kernel framing this is $\mathcal{Q}' = \mathcal{Q} \vee \sigma(Z)$ with $Z = $ temperature (and possibly higher-order weather variables at T2 fill time). The prior-supported candidate per (S10c)'s under-dispersion signature, (S11)'s social-weekly localisation (which leaves room for a secondary climatological signal), and the `mitacs-clim-gap-nonstationary` record (hour-flat $\sim +1000$ MW persistent post-cutoff bias) is hourly temperature from ECCC (Environment and Climate Change Canada).

**T1 phase_a fill is BLOCKED on ECCC data acquisition** — a pre-experiment infrastructure cost (station selection vs province-wide aggregation, alignment to Ontario demand hourly timestamps, missing-data handling, decile binning vs continuous) that must land as next-session infrastructure work before T1 phase_a can be filled. **`current_node_id` remains `null`** (T1 will become `current_node` when its phase_a is filled; the state-machine advancement is data-acquisition-gated, not amendment-gated). **`state` remains `active`** — the thread is now active on the temperature arm. T2 phase_a fill is contingent on T1.R-B1T per `branching_rules.T1`; T3 is optional contingent on T1/T2 CI structure. The thread's research question is unchanged; this is a continuation of path (i) candidate-$Z$ testing extended to exogenous $Z$, not a new question.

**P2 (synthetic Gaussian-DGP calibration) and NPMLEmix (Deb, Saha, Guntuboyina & Sen 2022) remain un-pursued framework paths; A4 does not close them.** P2 remains reachable via the original `Q1C.R-C1C` / `Q1B.R-C1B` / `Q1A.R-C1A` routes (none of which fired). NPMLEmix is reachable as a handoff target from `T1.R-C1T` ("temperature doesn't help" $\Rightarrow$ alternative-$Z$ / NPMLEmix / synthetic-DGP) per A4's outcome-categories spec. The temperature arm is opened **without** prejudging that exogenous $Z$ closes the gap; if T1 fires R-C1T, the thread will face an exogenous-$Z$-also-bounded reading that strengthens (rather than refutes) the joint identifiability-obstruction record and routes onward to methods-novelty (NPMLEmix) or to the third framework-prescribed path (synthetic DGP calibration via P2).

**Active nodes (post-A4):** none — T1/T2/T3 are `planned`; T1 will advance to `active` when its phase_a is filled post-ECCC-acquisition. **Planned:** P2, T1, T2, T3. **Closed:** Q1C. **Settled:** P1 (R-A landslide), Q1A (R-B1A), Q1B (R-B1B substantive favorable), Q1B' (R-B1B' aligned). **Exhausted-pre-data:** Q1A' (per A3). **`current_node_id` = null** (state machine awaiting T1 phase_a fill).

**Amendment chain through A4:** A1 `prior_tree_hash = 9a5647a5...` (added Q1A'); A2 `prior_tree_hash = 91d529c9...` (added Q1B' tightening sibling); A3 `prior_tree_hash = 164f8bb9...` (closed Q1A' pre-data); A4 `prior_tree_hash = 03e8664c...` (grafted T1/T2/T3 temperature-arm sub-tree). Post-A4 `body_sha256 = 7506d272399de0816083171a867919ffc7adaf558290f1d3e09f982efe80558b` (`git_sha = ddb2f519...`). State-history chain through **S5** unchanged: S1 `prior_hash = 3fe35739...` $\to$ S2 `prior_hash = c97c3ad5...` $\to$ S3 `prior_hash = 21a8bb20...` $\to$ S4 `prior_hash = 173b3d58...` $\to$ S5 `prior_hash = 5c8f5e7d...`. The state_history is unchanged by A4 (no arbiter rendered; no state advancement triggered); A4 is an append-internal amendment recorded only in the `amendments` block and verified via the `prior_tree_hash` chain.
