## resolution-paths-thread

### The question

This thread asks which of the three framework-prescribed resolution paths closes the residual $\chi^2 \approx 954$ of the $\kappa_1$ predictor on post-cutoff Ontario data, where "closure" is defined as cumulative explained share $> 0.80$ against the Q2A baseline. The thread's scope is narrow by design: it tests the $\sigma$-algebra-enrichment axis with auxiliary information $Z$, and it does **not** re-test the distributional-class axis — that lever was bounded by [2026-05-26_q2a-richer-family] (S9) and [2026-05-26_q2b-non-distributional-decomposition] (S10). The framework grounding is the identifiability obstruction parked at `~/Research/Mathematics/Resolvent_Framework/notes/unsorted/disintegration_diagnostic.md` (with its `disintegration_diagnostic_audit_correction.md` companion): per Bergna et al. (2026, Prop 1), Heckman & Singer (1984), and Allahverdyan (2020), structured-noise and noisy-structure are observationally indistinguishable from $\kappa_Q$ alone. The joint S9+S10 verdict is the applied confirmation of that obstruction; resolution requires breaking the tie via information outside $\kappa_Q$, i.e. enriching the conditioning $\sigma$-algebra. All results are attribution-grade — adding $Z$ to a predictor changes its conditioning $\sigma$-algebra, so this thread does not yield deployable forecasts but rather a Bayes-risk decomposition of the residual non-Gaussianity.

### The tree and the rules

Five nodes were planned initially; **amendment A1 (2026-05-27)** added a sixth, **Q1A'**, as a non-gating sibling of Q1A under P1.R-A; **amendment A2 (2026-05-29)** adds a seventh, **Q1B'**, as a non-gating sibling of Q1B under R-A — an empirical CI-tightening run at `N_PARTICLES=400` (vs Q1B's $200$) that brackets whether Q1B's wide cumulative-$\chi^2$ CI $[+8.06, +484.4]$ (width $476.358$, spans $\sim 60\times$) is dominated by SMC variance or by eval-window-noise. **P1** (Patra–Sen per stratum) runs first as a localization diagnostic: for each of five natural stratification axes (demand quantile, time-of-day, season, day-type, hour-of-week), compute the plain Patra & Sen (2016) lower bound $\hat\alpha_L$ per stratum on the standardized residuals of Q1's M1 (Student-t kernel) predictor, with 1000 paired-day bootstrap CIs. The verdict statistic is $\max_{\text{axis}} \mathrm{range}(\hat\alpha_L)$: R-A at $> 0.20$ (localizable), R-B in $(0.10, 0.20]$, R-C at $< 0.10$ (orthogonal). Localize-first is deliberate — per the framework prescription, candidate-$Z$ testing without first checking that $Z$ is concentrable on natural strata wastes signal. **Q1A/Q1B** re-fit M3 (mixture-2-Gaussian) with $Z$ derived from P1's strongest (resp. second-strongest) axis under R-A/R-B; **Q1C** tests temperature as a prior-supported fallback under R-C, motivated by `mitacs-clim-gap-nonstationary` and `mitacs-postcovid-probe`. **P2** is a synthetic-Gaussian-DGP calibration backstop, fired only if all live R-A branches collapse to R-C1A/B/C. **Q1A'** runs *in parallel* with Q1A as a data-driven Z-recovery consistency check (SIR vs FastICA head-to-head); **Q1B'** runs *in parallel* with Q1B as a CI-tightening replication of Q1B's winning configuration at denser SMC ($M = 400$); both Q1A' and Q1B' have all three outcomes mapped to `null` in `branching_rules` (independent verdicts; non-gating w.r.t. the thread state machine). The closure rule (for the gating sequence) is numeric, following Q2B's convention: $$\text{share} = \frac{\chi^2_{\text{base}} - \chi^2_{\text{after}}}{\chi^2_{\text{base}} - \chi^2_{\text{R-A2 cut}}},$$ with cumulative share $> 0.80$ across tested $Z$'s $\Rightarrow$ RESOLVED, otherwise $\Rightarrow$ EXHAUSTED-UNRESOLVED. The methods-novelty branch was determined dead by `notes/literature/2026-05-26_stratified-patra-sen-scout.md`: Deb, Saha, Guntuboyina & Sen (2022, JASA; NPMLEmix) provide the canonical covariate-dependent generalization, so this thread uses plain Patra–Sen per stratum — sufficient for the registered verdict without invoking NPMLEmix's Mosek dependency.

### Current position (post-S4, post-A2; A2 written 2026-05-29T22:00:00-07:00)

**Q1B SETTLED R-B1B** (arbiter `body_sha256 = d8650cb7...` against Q1B phase_a `0602f0c0...`), mechanical R-B1B + substantive R-B1B_SUBSTANTIVE_FAVORABLE per arbiter aggregate SETTLED. The realized verdict statistic $\text{better\_chi}^2 = 662.2233$ with paired-day bootstrap 95% CI $[468.094, 944.452]$ sits unambiguously interior to the registered $(228, 2284]$ band — $\sim 2.05\times$ clear of R-A1B on the low side and $\sim 2.42\times$ clear of R-C1B on the high side; both point and full CI inside the ambiguous band, not borderline. The head-to-head between $Z_2^{(b)}$ (binary overnight indicator) and $Z_2^{(c)}$ (4-level categorical per P1's production binning) is a **near-tie**: $\chi^2_{\text{diff}} = +33.78$ $[-53.19, +124.47]$, paired SE $44.71$, $|\text{diff}|/\text{SE} = 0.756 < 1.0$. Per phase_a near-tie rule, "no statistically-discernible winner"; $Z_2^{(c)}$ wins by parsimony-of-point-estimate convention. **The substantive headline** is the cumulative reduction above $Z_c$ carryforward: $\Delta\chi^2 = 290.29$ $[8.06, 484.42]$ $= 40.07\%$ of the remaining gap-to-close to R-A1B ($725.84$ between Q1A baseline $952.5167$ and R-A1B cut $228$). For comparison, Q1A's $Z$-lever closed $0.18\%$ of the gap ($1.33$ $\chi^2$ units). **Q1B's $Z_2$ lever is $218\times$ Q1A's in absolute $\chi^2$ units and $\sim 220\times$ as a share-of-gap.** Reproduction checks at both anchors are EXACT (Q2A no-Z baseline reproduced to 4 decimals; Q1A $Z_c$-only baseline reproduced to 8 decimals — strongest dual-anchor reproduction in the workflow's history). **Per-cell decomposition** corroborates P1 at finer resolution: Q1A's dominant saturday__zc6 cell ($\chi^2 = 1634$, $\hat\alpha_L = 0.61$) splits as morning_ramp ($967.28$) > evening ($399.77$) > afternoon ($328.09$); the morning_ramp sub-stratum alone exceeds the pooled Q1B marginal $662.22$ by $46\%$. The contamination IS where P1 said it would be AND has internal time_of_day structure single-axis P1 ranking could not isolate. **Sixth instance of fourth-named-failure-mode in a new shape variant**: mixed scoring decomposition across forecast surfaces — outcome category + near-tie shape match DA; winner-direction matches proponent at point; cumulative-reduction magnitude (290 $\chi^2$) sits orders of magnitude from both sides' implicit priors (proponent expected $\sim 772$ $\chi^2$ closure; DA expected $\sim 5$). Realized data in a previously-uncharted cumulative-reduction regime.

**State advancement S4 (`prior_hash = 173b3d58...`, new `body_sha256 = 91d529c9...`).** Q1B.status: active → settled (`result: R-B1B`, phase_a_path filled). `current_node_id`: Q1B → null. **The thread is PAUSED pending user decision on amendment direction**, not resolved and not exhausted. Per `branching_rules.Q1B.R-B1B = null`, R-B1B does not auto-advance the state machine. Per `root.closure_rule`, the realized cumulative share $0.40 < 0.80$ threshold — the thread does NOT auto-resolve. **The S3 EXHAUSTED-UNRESOLVED pre-flag is DOWNGRADED**, not auto-fired: its premise ("Q1B delivers similar 0.18%-of-gap magnitudes to Q1A") is empirically refuted by Q1B's $218\times$-Q1A realization. The thread is in a state the registered tree did not pre-anticipate: substantively informative + neither RESOLVED nor EXHAUSTED. The workflow shape mirrors Q2A's "first arbiter without mechanical/substantive gap" — a tree-shape-first observation that the realized data sits in a previously-uncharted regime relative to both forecasters' priors and the pre-registered closure logic. **No siblings closed** at the thread level (R-A1B mapped to null would-have-routed-to-thread-RESOLVED; R-C1B mapped to P2 which stays reachable via Q1C.R-C1C). Q1A' (parallel non-state-machine sibling per amendment A1) is unaffected.

**Framework reading sharpening (binding).** Joint S9+S10+S11+S12 was read in Q1A's arbiter and the S3 thread notes as "within-data $\sigma$-algebra-enrichment levers are empirically bounded at the $\sim 0.18\%$-of-gap scale." Q1B refutes that reading **as-stated**. The sharpened reading: **single-axis** within-data $\sigma$-algebra enrichment IS bounded near zero (Q1A's $0.18\%$); **joint multi-axis** within-data conditioning recovers MEANINGFULLY MORE (Q1B's $40.07\%$ on top of Q1A's carryforward). FULL closure to R-A1B is NOT achieved within the time-index alone (cumulative point $662$ still $2.9\times$ above R-A1B cut; lower CI $468$ still $2.05\times$ above), so the identifiability obstruction at the **full-closure** level remains a candidate reading — but the within-data lever space has more directions than single-axis P1 ranking suggested. Cross-axis joint stratification is the within-data direction the framework reading should now incorporate. (Framework-side audit backlog: `writeup/tex/missing_content_memo.tex` predates (S11) and (S12); (S13) compounds the backlog. CLAUDE.md "Settled findings since 2026-05-21" needs (S13) added at next refresh.)

**Active nodes (post-S4, post-A2):** Q1A' (non-gating, SDR/ICA consistency check; phase_a deferred per "Q1A' deferred" in CLAUDE.md pending FastICA selection-rule design refresh). **Planned (post-A2):** Q1B' (non-gating, CI-tightening replication of Q1B's winning $(Z_c, Z_2^{(c)})$ configuration at `N_PARTICLES=400`; phase_a not yet filled — preregister's next action), P2 (synthetic Gaussian-DGP calibration, reachable via Q1C.R-C1C). **Closed:** Q1C (sibling branch R-C of P1 did not fire). **Settled:** P1 (R-A landslide), Q1A (R-B1A), Q1B (R-B1B, substantive favorable). **current_node = null (paused; thread state machine unchanged by A2).** State history through **S4** (`prior_hash = 173b3d58...`, post-S4 `body_sha256 = 91d529c9...`); amendments through **A2** (`prior_tree_hash = 91d529c9...`, post-A2 `body_sha256 = 5c8f5e7d5a3640018ea99010079498e92e5c79e86d187881a7f3d9eccdd18fb9`; **A2 git_sha = 9ee4ac20...**). A1's `prior_tree_hash = 9a5647a5...` remains the chain anchor at the A1 step. The chain remains intact via `registry.py:234-256`.

**Why A2 sits between S4 and the (a)/(b)/(c) adjudication.** Q1B's headline `cumulative_chi2_improvement_above_Z_c` $= +290.3$ with $95\%$ CI $[+8.06, +484.4]$ has CI width $476.358$ — the ratio of upper to lower bound is $\sim 60\times$. Per user decision (d) locked at the end of session 2026-05-29-a, the (a)/(b)/(c) options surfaced in S4's coordinator-decision-space are each ranked differently depending on which CI endpoint is closer to truth: near the lower bound ($8.06$, essentially Q1A-scale), option (c) — the temperature/exogenous-$Z$ arm — becomes dominant because within-data joint conditioning would not be meaningfully different from single-axis; near the upper bound ($484.4$, $\sim 67\%$ closure), options (a)/(b) — third within-data $Z$ axis or cross-axis-joint thread spawn — become attractive because within-data joint conditioning would clearly carry signal. A2 brackets the CI width before the user picks. Q1B' re-runs Q1B's winning configuration at $M = 400$ SMC particles (vs Q1B's $M = 200$), holding everything else identical (library, fitter, seed convention, paired-day bootstrap, dual reproduction anchors Q2A $953.84$ and Q1A $952.5167$). Verdict is on the realized CI width: R-A1B' fires at width $\le 357.27$ ($\le 75\%$ of Q1B's $476.358$, i.e. SMC variance dominates and the lever works); R-B1B' is mixed $(357.27, 524.0]$; R-C1B' fires at width $> 524.0$ OR $|$point shift$| > 50$ $\chi^2$ (anomaly). Reproduction-check semantics at $M = 400$ are part of the measurement, not a gate: the existing `assert` at `__main__.py:1424/1454` (tol $50$ $\chi^2$) may halt execution if SMC variance at $M = 200$ was non-trivial in the parent fits — phase_a may carve out a catch-and-report (preregister's call when filling). Q1B' is **non-gating w.r.t. the thread state machine**: `branching_rules.Q1B' = {R-A1B': null, R-B1B': null, R-C1B': null}`, mirroring A1's Q1A' shape. The thread remains `state = active`, `current_node_id = null`; Q1B' settlement does NOT advance the tree. The (a)/(b)/(c) decision waits on Q1B' results.

### Coordinator decision space (deferred to user)

The thread-coordinator does NOT pre-commit the tree; the amendment-direction decision is the user's (and benefits from advisor input). Per the arbiter's `notes_for_thread_coordinator`, all three options below are framework-supported and the realized data does not statistically rank them:

- **(a) Thread amendment: third within-data $Z$ axis.** P1's remaining axes ranked by range: `season` ($0.135$), `demand_quantile` ($0.075$) — weaker prior support than `time_of_day` ($0.485$, used as $Z_2$) but the cross-axis joint hypothesis is newly-validated by Q1B's realization. Could register Q1D (third within-data $Z$ axis added to $(Z_c, Z_2^{(c)})$ carryforward) with adjusted `MIN_CELL_SIZE` for the finer cell partition.

- **(b) Spawn a new thread targeting cross-axis joint stratification systematically.** The framework-reading sharpening Q1B delivered (single-axis vs joint multi-axis as distinct lever classes) suggests a new line of inquiry: what is the cumulative limit of within-data joint conditioning? Multi-axis sweeps could be pre-registered as a thread root rather than as single-axis amendments to this thread.

- **(c) Pivot to the temperature/exogenous-$Z$ arm.** Q1C reopening amendment OR new thread per the framework-prescribed resolution paths in `disintegration_diagnostic.md`. Full closure to R-A1B is NOT achieved within the time-index (cumulative point $662$ still $2.9\times$ above R-A1B cut; lower CI $468$ still $2.05\times$ above). The `clim-gap-nonstationary` memory continues to point at temperature as the strongest a-priori candidate for the residual $60\%$ of the gap.

Temperature prior status: still candidate for full closure beyond within-data joint conditioning. Q1B's $40\%$-of-gap closure leaves $60\%$ unexplained; the `clim-gap-nonstationary` prior continues to be the strongest a-priori candidate for the residual. The (a)–(c) ordering implicitly ranks within-data axes first vs exogenous-$Z$ first.
