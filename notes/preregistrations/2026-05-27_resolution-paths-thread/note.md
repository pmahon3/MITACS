## resolution-paths-thread

### The question

This thread asks which of the three framework-prescribed resolution paths closes the residual $\chi^2 \approx 954$ of the $\kappa_1$ predictor on post-cutoff Ontario data, where "closure" is defined as cumulative explained share $> 0.80$ against the Q2A baseline. The thread's scope is narrow by design: it tests the $\sigma$-algebra-enrichment axis with auxiliary information $Z$, and it does **not** re-test the distributional-class axis — that lever was bounded by [2026-05-26_q2a-richer-family] (S9) and [2026-05-26_q2b-non-distributional-decomposition] (S10). The framework grounding is the identifiability obstruction parked at `~/Research/Mathematics/Resolvent_Framework/notes/unsorted/disintegration_diagnostic.md` (with its `disintegration_diagnostic_audit_correction.md` companion): per Bergna et al. (2026, Prop 1), Heckman & Singer (1984), and Allahverdyan (2020), structured-noise and noisy-structure are observationally indistinguishable from $\kappa_Q$ alone. The joint S9+S10 verdict is the applied confirmation of that obstruction; resolution requires breaking the tie via information outside $\kappa_Q$, i.e. enriching the conditioning $\sigma$-algebra. All results are attribution-grade — adding $Z$ to a predictor changes its conditioning $\sigma$-algebra, so this thread does not yield deployable forecasts but rather a Bayes-risk decomposition of the residual non-Gaussianity.

### The tree and the rules

Five nodes were planned initially; **amendment A1 (2026-05-27)** added a sixth, **Q1A'**, as a non-gating sibling of Q1A under P1.R-A; **amendment A2 (2026-05-29)** added a seventh, **Q1B'**, as a non-gating sibling of Q1B under R-A — an empirical CI-tightening run at `N_PARTICLES=400` (vs Q1B's $200$) bracketing whether Q1B's wide cumulative-$\chi^2$ CI $[+8.06, +484.4]$ (width $476.358$, spans $\sim 60\times$) is dominated by SMC variance or by eval-window-noise. **P1** (Patra–Sen per stratum) runs first as a localization diagnostic: for each of five natural stratification axes (demand quantile, time-of-day, season, day-type, hour-of-week), compute the plain Patra & Sen (2016) lower bound $\hat\alpha_L$ per stratum on the standardized residuals of Q1's M1 (Student-t kernel) predictor, with 1000 paired-day bootstrap CIs. The verdict statistic is $\max_{\text{axis}} \mathrm{range}(\hat\alpha_L)$: R-A at $> 0.20$ (localizable), R-B in $(0.10, 0.20]$, R-C at $< 0.10$ (orthogonal). Localize-first is deliberate — per the framework prescription, candidate-$Z$ testing without first checking that $Z$ is concentrable on natural strata wastes signal. **Q1A/Q1B** re-fit M3 (mixture-2-Gaussian) with $Z$ derived from P1's strongest (resp. second-strongest) axis under R-A/R-B; **Q1C** tests temperature as a prior-supported fallback under R-C, motivated by `mitacs-clim-gap-nonstationary` and `mitacs-postcovid-probe`. **P2** is a synthetic-Gaussian-DGP calibration backstop, fired only if all live R-A branches collapse to R-C1A/B/C. **Q1A'** runs *in parallel* with Q1A as a data-driven Z-recovery consistency check (SIR vs FastICA head-to-head); **Q1B'** runs *in parallel* with Q1B as a CI-tightening replication of Q1B's winning configuration at denser SMC ($M = 400$); both Q1A' and Q1B' have all three outcomes mapped to `null` in `branching_rules` (independent verdicts; non-gating w.r.t. the thread state machine). The closure rule (for the gating sequence) is numeric, following Q2B's convention: $$\text{share} = \frac{\chi^2_{\text{base}} - \chi^2_{\text{after}}}{\chi^2_{\text{base}} - \chi^2_{\text{R-A2 cut}}},$$ with cumulative share $> 0.80$ across tested $Z$'s $\Rightarrow$ RESOLVED, otherwise $\Rightarrow$ EXHAUSTED-UNRESOLVED. The methods-novelty branch was determined dead by `notes/literature/2026-05-26_stratified-patra-sen-scout.md`: Deb, Saha, Guntuboyina & Sen (2022, JASA; NPMLEmix) provide the canonical covariate-dependent generalization, so this thread uses plain Patra–Sen per stratum — sufficient for the registered verdict without invoking NPMLEmix's Mosek dependency.

### Current position (post-S5; S5 written 2026-05-28T10:30:00-07:00)

**Q1B' SETTLED R-B1B'** (arbiter `body_sha256 = 1ee09afa...` against Q1B' phase_a `7ed3eea3...`), **mechanical = substantive, ALIGNED on R-B1B'** (no `_SUBSTANTIVE_FAVORABLE`/`_UNFAVORABLE` suffix; the substantive content sharpens the framework reading but does not pull the verdict toward either neighboring category). The realized verdict statistic is the cumulative-$\chi^2$ CI **width**: realized $95\%$ CI on $\text{cumulative\_chi2\_improvement\_above\_Z}_c = [-34.576, +471.622]$, **width $= 506.198$ $\chi^2 \in (357.27, 524.0]$**. Realized $\text{better\_chi}^2 = 679.128$; $|\text{better\_chi}^2_{\text{point}} - 662.223| = 16.905 \ll \text{tol} = 50$ — point-stability sub-criterion cleared by $\sim 3\times$ headroom. Both R-B1B' conjuncts (CI-width band + point-stability) cleared unambiguously. R-A1B' (corroboration: width $\le 357.27$) NOT cleared ($148.93$ $\chi^2$ above the cut, $\sim 42\%$ of the cut value). R-C1B' (falsification: width $> 524.0$ OR $|\text{point-shift}| > 50$) NOT cleared on either disjunct (width is $17.80$ $\chi^2$ below the cut, $3.4\%$ headroom; point-shift is $33.10$ $\chi^2$ below tol). **The realized direction is $+29.84$ $\chi^2$ WIDENING ($+6.3\%$ vs Q1B's $476.358$), OPPOSITE both forecasters' narrowing predictions:** proponent forecast R-A1B' (width $338$ $[290, 395]$, $\ge 25\%$ narrowing); DA forecast R-B1B' (width $419$ $[370, 480]$, $\sim 12\%$ narrowing). DA wins on the registered outcome category (R-B1B' mechanical match); proponent's R-A1B' falsified. But **both forecasters' direction-of-magnitude forecasts on the verdict-metric are FALSIFIED at the central tendency**: the mechanical R-B1B' band is wide enough to absorb both predicted narrowings AND the realized widening — the band itself is the unfalsifiability zone for direction errors on this metric. The realized width sits at $89.3\%$ of the way through the R-B1B' band ($8.4\times$ closer to R-C1B' than to R-A1B') — a structurally different R-B1B' shape than Q1B's settled R-B1B (which sat unambiguously interior to its band, $2.05\times$ clear of R-A1B on the low side per S4). Q1B' R-B1B' fires at the OPPOSITE end of its band, near the widening-direction edge, consistent with the $+6.3\%$ widening central finding.

**Reproduction checks PASS at both anchors**. Check #1 (no-Z anchor): $\chi^2 = 959.065$ vs Q2A settled $953.84$; $|\text{diff}| = 5.225 < 50$, OK ($\sim 10\%$ of tol). Check #2 (Q1A $Z_c$-only carryforward anchor): $\chi^2 = 997.112$ vs Q1A settled $952.517$; $|\text{diff}| = 44.595 < 50$, OK ($11\%$ headroom). The Q1A-anchor drift consuming $89\%$ of tol budget is itself empirical evidence that SMC particle noise at $N = 200$ IS non-trivial at the $Z_c$-stratified parent layer (but NOT at the no-Z pooled layer where headroom is $\sim 89\%$); sharpens the framework reading from S13 that joint-stratification finite-sample noise is the residual within-data driver of CI width at the parent fits, not the SMC propagation step itself. Both BLOCKING asserts at `__main__.py:1424/1454` PASSED.

**Framework reading EXTENSION (binding for the kernel-family-agnostic theory memo + CLAUDE.md framework section at next refresh).** Q1B' provides **methodological-side** corroboration of S13's substantive finding that within-data tightening levers are bounded. S13 established this at the **substantive level** (single-axis bounded near zero per S11+S12; joint multi-axis bounded at higher magnitude per S13); Q1B' establishes it at the **methodological level** (SMC particle-count doubling does NOT narrow the realized CI; eval-window-noise dominates over SMC propagation noise in the Q1B$(Z_c, Z_2^{(c)})$ configuration). **Joint S9+S10+S11+S12+S13+Q1B' is now a THREE-LAYER CORROBORATION** of the identifiability obstruction from `disintegration_diagnostic.md`: distributional-class via S9+S10; $\sigma$-algebra-enrichment via S11+S12+S13; methodological tightening via Q1B'. The boundedness reading is sharpened from S13's "single-axis bounded near zero; joint multi-axis bounded at higher magnitude but full closure requires auxiliary information beyond the time index" to **"single-axis bounded near zero; joint multi-axis bounded at higher magnitude; methodological tightening (SMC particle count) cannot narrow the CI further; full closure requires auxiliary information beyond the time index."** This is the durable framework correction Q1B' delivered on the methodological side.

**User decision (d) premise CONFIRMED.** The locked end-of-session-2026-05-29-a decision (d) — run Q1B' to bracket whether Q1B's wide CI was SMC-driven or eval-window-driven *before* (a)/(b)/(c) adjudication — is now answered. Q1B' confirms the user's framing: **Q1B's wide CI is intrinsic to data, not estimator.** The (a)/(b)/(c) decision must now proceed on substantive grounds **on the wide CI** — within-data tightening is empirically impossible. **Seventh named-failure-mode instance, new shape variant**: OUTCOME-CATEGORY MATCHES ONE SIDE (DA), BOTH SIDES FALSIFIED ON DIRECTION-OF-MAGNITUDE on the verdict-metric. The mechanical band absorbs the disagreement; the band itself is the unfalsifiability zone. Future eval-window-dominance forecasts should pre-register more conservative width-change predictions (closer to no-change-or-slight-widening rather than 12% narrowing).

**State advancement S5** (`prior_hash = 5c8f5e7d...`, post-S5 `body_sha256 = 164f8bb95e347e2db90e26ac1bc46c0dd4fac42d07733c0ba9651b841fd8fe15`, `git_sha = b2a6f898...`). Q1B'.status: `planned` → `settled`; Q1B'.result: `R-B1B'`; Q1B'.phase_a_path filled (`2026-05-29_q1b-prime-smc-tightening/phase_a.yaml`); `current_node_id`: `null` → `null` (**UNCHANGED** per amendment A2's non-gating-sibling design — `branching_rules.Q1B' = {R-A1B': null, R-B1B': null, R-C1B': null}`; settlement does NOT advance the state machine). Thread `state` remains `active` (neither RESOLVED nor EXHAUSTED). **No siblings closed at thread level** — all three Q1B' outcome branches map to null per A2; P2 remains reachable via Q1C.R-C1C and is NOT closed. **Active nodes (post-S5):** Q1A' (non-gating; registry status AWAITING-B; FastICA selection-rule defect; phase_a deferred per "Q1A' deferred" in CLAUDE.md pending design refresh). **Planned (post-S5):** P2 (synthetic Gaussian-DGP calibration; reachable via Q1C.R-C1C). **Closed:** Q1C (sibling of P1.R-A; sibling-not-fired at S2). **Settled:** P1 (R-A landslide), Q1A (R-B1A), Q1B (R-B1B substantive favorable), Q1B' (R-B1B' aligned). **current_node = null (paused; thread state machine UNCHANGED by S5; awaiting user adjudication of the main Q1B branch's (a)/(b)/(c) three-way amendment decision).**

State history through **S5** (chain: S1 `prior_hash = 3fe35739...` → S2 `prior_hash = c97c3ad5...` → S3 `prior_hash = 21a8bb20...` → S4 `prior_hash = 173b3d58...` → S5 `prior_hash = 5c8f5e7d...`). Amendments through **A2** (A1 `prior_tree_hash = 9a5647a5...`; A2 `prior_tree_hash = 91d529c9...`). The chain remains intact via `registry.py:234-256`; chain verified for both the thread and Q1B''s phase_a directory.

**INSPECTION-ONLY citation caveat (Q1B').** Q1B''s `result.yaml` carries `grade_demotion_reason: PROVENANCE-GRADE: INSPECTION-ONLY` (per Q1B''s pre-registered hardcoded `.txt` output path colliding with Q1B's SETTLED CLAIM-GRADE artifact; Check P missed this string in the parameterization substitution set per commit `8cd2935`). The `.txt` was restored from `HEAD` by user adjudication; `result.yaml` + pickle ARE the durable record, both produced by audited-PRODUCTION-PATH code with all BLOCKING gates passed. The verdict numerics are settled and non-revisable; the citation grade is non-removable absent re-emission. Downstream memory file (`mitacs-q1b-prime-smc-tightening.md`, arbiter-recommended) and CLAUDE.md (S14) update must explicitly carry the INSPECTION-ONLY caveat (out of thread-coordinator scope; recorded here for the next dispatch).

### Coordinator decision space (deferred to user; INFORMED by Q1B', not auto-decided)

The thread-coordinator does NOT pre-commit the tree; the amendment-direction decision is the user's (and benefits from advisor input). Per the Q1B' arbiter's `notes_for_thread_coordinator` (explicitly non-prejudging), all three options below remain framework-supported. Q1B' has now confirmed the wide-CI premise on which option ordering depends — within-data tightening cannot narrow Q1B's CI further; the (a)/(b)/(c) choice proceeds on substantive grounds across the realized wide CI $[+8.06, +484.4]$, not against a narrower CI as proponent had hoped.

- **(a) Thread amendment: third within-data $Z$ axis.** P1's remaining axes ranked by range: `season` ($0.135$), `demand_quantile` ($0.075$) — weaker prior support than `time_of_day` ($0.485$, used as $Z_2$) but the cross-axis joint hypothesis is newly-validated by Q1B's $40\%$-of-gap realization. Could register Q1D (third within-data $Z$ axis added to $(Z_c, Z_2^{(c)})$ carryforward) with adjusted `MIN_CELL_SIZE` for the finer cell partition. **Q1B' shifts the prior on (a):** with within-data SMC tightening empirically negative, the third-axis additional closure prior shrinks too — diminishing returns within the time index.

- **(b) Spawn a new thread targeting cross-axis joint stratification systematically.** The framework-reading sharpening Q1B delivered (single-axis vs joint multi-axis as distinct lever classes) and Q1B''s methodological confirmation suggest a new line of inquiry: what is the cumulative limit of within-data joint conditioning? Multi-axis sweeps could be pre-registered as a thread root rather than as single-axis amendments to this thread. **Q1B' makes (b) more systematic-investigation-friendly** — eval-window-noise being the residual driver argues for a sweep design that explicitly models eval-window variance.

- **(c) Pivot to the temperature/exogenous-$Z$ arm.** Q1C reopening amendment OR new thread per the framework-prescribed resolution paths in `disintegration_diagnostic.md`. Full closure to R-A1B is NOT achieved within the time-index (cumulative point $662$ still $2.9\times$ above R-A1B cut; lower CI $468$ still $2.05\times$ above). The `clim-gap-nonstationary` memory continues to point at temperature as the strongest a-priori candidate for the residual $60\%$ of the gap. **Q1B' upgrades (c)'s priority:** the within-data lever space is now bounded both substantively (S11+S12+S13) AND methodologically (Q1B') — auxiliary information beyond the time index is the only remaining direction the framework prescribes for full closure. Q1B' does not name (c) the winner; it sharpens the prior that (c) is required for full closure, while (a)/(b) remain valid investigations into the structure of within-data conditioning even if full closure is unreachable from them.

Temperature prior status: still candidate for full closure beyond within-data joint conditioning. Q1B's $40\%$-of-gap closure leaves $60\%$ unexplained; Q1B' confirms SMC-tightening cannot recover within-data variance further; the `clim-gap-nonstationary` prior continues to be the strongest a-priori candidate for the residual. The (a)–(c) ordering implicitly ranks within-data axes first vs exogenous-$Z$ first; Q1B' has not reordered the options, but it has weighted the relative priors.
