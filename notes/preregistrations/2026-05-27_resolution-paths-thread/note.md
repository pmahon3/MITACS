## resolution-paths-thread

### The question

This thread asks which of the three framework-prescribed resolution paths closes the residual $\chi^2 \approx 954$ of the $\kappa_1$ predictor on post-cutoff Ontario data, where "closure" is defined as cumulative explained share $> 0.80$ against the Q2A baseline. The thread's scope is narrow by design: it tests the $\sigma$-algebra-enrichment axis with auxiliary information $Z$, and it does **not** re-test the distributional-class axis — that lever was bounded by [2026-05-26_q2a-richer-family] (S9) and [2026-05-26_q2b-non-distributional-decomposition] (S10). The framework grounding is the identifiability obstruction parked at `~/Research/Mathematics/Resolvent_Framework/notes/unsorted/disintegration_diagnostic.md` (with its `disintegration_diagnostic_audit_correction.md` companion): per Bergna et al. (2026, Prop 1), Heckman & Singer (1984), and Allahverdyan (2020), structured-noise and noisy-structure are observationally indistinguishable from $\kappa_Q$ alone. The joint S9+S10 verdict is the applied confirmation of that obstruction; resolution requires breaking the tie via information outside $\kappa_Q$, i.e. enriching the conditioning $\sigma$-algebra. All results are attribution-grade — adding $Z$ to a predictor changes its conditioning $\sigma$-algebra, so this thread does not yield deployable forecasts but rather a Bayes-risk decomposition of the residual non-Gaussianity.

### The tree and the rules

Five nodes were planned initially; **amendment A1 (2026-05-27)** added a sixth, **Q1A'**, as a non-gating sibling of Q1A under P1.R-A. **P1** (Patra–Sen per stratum) runs first as a localization diagnostic: for each of five natural stratification axes (demand quantile, time-of-day, season, day-type, hour-of-week), compute the plain Patra & Sen (2016) lower bound $\hat\alpha_L$ per stratum on the standardized residuals of Q1's M1 (Student-t kernel) predictor, with 1000 paired-day bootstrap CIs. The verdict statistic is $\max_{\text{axis}} \mathrm{range}(\hat\alpha_L)$: R-A at $> 0.20$ (localizable), R-B in $(0.10, 0.20]$, R-C at $< 0.10$ (orthogonal). Localize-first is deliberate — per the framework prescription, candidate-$Z$ testing without first checking that $Z$ is concentrable on natural strata wastes signal. **Q1A/Q1B** re-fit M3 (mixture-2-Gaussian) with $Z$ derived from P1's strongest (resp. second-strongest) axis under R-A/R-B; **Q1C** tests temperature as a prior-supported fallback under R-C, motivated by `mitacs-clim-gap-nonstationary` and `mitacs-postcovid-probe`. **P2** is a synthetic-Gaussian-DGP calibration backstop, fired only if all live R-A branches collapse to R-C1A/B/C. **Q1A'** runs *in parallel* with Q1A as a data-driven Z-recovery consistency check (SIR vs FastICA head-to-head); its verdict is independent and does not advance the thread state machine. Q1A remains the gating node for `current_node_id` progression through R-B1A → Q1B. The closure rule (for the gating sequence) is numeric, following Q2B's convention: $$\text{share} = \frac{\chi^2_{\text{base}} - \chi^2_{\text{after}}}{\chi^2_{\text{base}} - \chi^2_{\text{R-A2 cut}}},$$ with cumulative share $> 0.80$ across tested $Z$'s $\Rightarrow$ RESOLVED, otherwise $\Rightarrow$ EXHAUSTED-UNRESOLVED. The methods-novelty branch was determined dead by `notes/literature/2026-05-26_stratified-patra-sen-scout.md`: Deb, Saha, Guntuboyina & Sen (2022, JASA; NPMLEmix) provide the canonical covariate-dependent generalization, so this thread uses plain Patra–Sen per stratum — sufficient for the registered verdict without invoking NPMLEmix's Mosek dependency.

### Current position

**Q1A SETTLED R-B1A** (arbiter `body_sha256 = 6b49d913...` against Q1A phase_a `a2b13f6d...`). The realized verdict statistic $\text{better\_chi}^2 = 952.5167$ with paired-day bootstrap 95% CI $[625.34, 1342.33]$ sits comfortably inside the registered $(228, 2284]$ ambiguous band — $\sim 4\times$ clear of R-A1A on the low side and $\sim 2.4\times$ clear of R-C1A on the high side. R-B1A fires unambiguously. The devil's-advocate wins the outcome category (proponent's R-A1A is decisively falsified: forecast $\chi^2_{\min} = 200$ with CI $[130, 320]$ entirely below the realized lower CI $625$); on within-bucket point forecasts, DA's $\chi^2_{Z_c} = 950$ vs realized $952.5$ is a $0.3\%$ residual — the single best pre-registered numeric point forecast in the workflow's history. The head-to-head between $Z_b$ (binary weekend carve-out) and $Z_c$ (8-level hour-of-week stratification) is a **near-tie**: $\chi^2_{\text{diff}} = +33.5$ $[-93, +143]$ straddles zero (paired SE $56.84 > |\text{diff}|$). The **substantive headline is the Z-lever marginal share**: unconditioned baseline $953.84$ drops to $952.52$ under the better Z-conditioned candidate, a reduction of $1.33$ $\chi^2$ units out of the $725.84$ gap-to-close — a **share of $0.18\%$**. Per-cell $\chi^2$ corroborates P1's hot-zone identification (saturday__z6 single-cell $\chi^2 = 1634$, exceeding the pooled marginal; weekday cold-strata bins 0–4 all clear R-A1A within cell at $\chi^2 = 37$–$78$) but reveals a **family-capacity ceiling**: the mixture-2-Gaussian with tail weights $w_2 = 0.20$–$0.40$ in the hot cells cannot absorb the within-cell heterogeneity. Joint S9+S10+S11+Q1A picture corroborates the identifiability obstruction from the $\chi^2$ side — within-data $\sigma$-algebra enrichment via the strongest P1 axis (hour_of_week) is empirically bounded near zero. **Fifth instance of fourth-named-failure-mode in a NEW SHAPE VARIANT**: mechanical + within-bucket point forecasts both match DA; head-to-head direction is point-correct for proponent but realized direction is within paired SE (statistically zero).

**State advancement S3 (2026-05-27T20:11:35-07:00, `prior_hash = 21a8bb20...`).** Q1A.status: active → settled (`result: R-B1A`, phase_a_path filled). Q1B.status: planned → active. `current_node_id`: Q1A → Q1B. **No siblings closed** at the thread level — `branching_rules.Q1A.R-A1A` maps to null (would have routed to thread RESOLVED; did not fire); `branching_rules.Q1A.R-C1A` maps to P2 which remains reachable via Q1B.R-C1B and Q1C.R-C1C and is therefore NOT closed. Q1A' (parallel non-state-machine sibling per amendment A1) is unchanged. **Carryforward for Q1B**: $Z_1 = Z_c$ (8-level hour-of-week stratification) is the parsimony-preferred point-estimate winner used as Q1B's cumulative baseline, recorded in `nodes.Q1B.notes` with the **near-tie caveat** that $Z_b$ and $Z_c$ are statistically indistinguishable on the verdict-metric. Q1B's phase_a author should NOT treat $Z_c$ as definitively the right $Z_1$.

**Active nodes:** Q1B (gating, $Z_2$ from P1's second-strongest axis time_of_day at range $0.485$, cumulative $\chi^2$ against the Z_c-conditioned baseline) and Q1A' (non-gating, SDR/ICA consistency check). **Planned:** P2 (synthetic Gaussian-DGP calibration, reachable via Q1B.R-C1B and Q1C.R-C1C). **Closed:** Q1C (sibling branch R-C of P1 did not fire). **Settled:** P1 (R-A landslide), Q1A (R-B1A). **Trajectory pre-flag (advisory, per S3 notes):** with the Q1A Z-lever at $0.18\%$ of the gap and P1's second-strongest axis (time_of_day) re-partitioning information already in the time index, the prior on Q1B closing the gap via the same lever is weaker than Q1A's already-weak prior. If Q1B fires R-B1B / R-C1B, the thread approaches EXHAUSTED-UNRESOLVED; R-C1B routes to P2, and a thread amendment to reopen Q1C (temperature) or a new thread targeting temperature directly would be the natural next move outside the registered branching. State history through **S3** (`prior_hash = 21a8bb20...`, new `body_sha256 = 173b3d58...`); amendments through **A1** (`prior_tree_hash = 9a5647a5...`). The chain remains intact via `registry.py:234-256`.
