## distributional-class-thread

### The question and scope

This thread asked which predictive-distribution class would render the iterated $\kappa_1$-predictor marginally PIT-calibrated on post-cutoff Ontario data, and whether the cross-scale coherence defect ([2026-05-26_multiscale-direct-h-step-v2]'s $D_{\mathrm{RATIO}} = 28.96$) survives the kernel-family lift. The root was [2026-05-26_pit-calendar-fingerprint]'s SETTLED-AMBIGUOUS verdict, which documented the iterated Gaussian forecast at $\chi^2 = 22{,}329$ vs uniform PIT (critical $16.92$) with 67% of mass in extreme bins — the direct empirical consequence of treating the strongly non-Gaussian $\kappa_1$ innovation (excess kurtosis $24$–$33$, per `mitacs-rebaseline-facts`) as Gaussian. **Scope was registered narrow by design**: the thread tests the *predictive-distribution-class axis only*. It does **not** test $\sigma$-algebra enrichment with auxiliary $Z$ (weather, secular drift, hour-of-week), and it does **not** propose a v3 registered predictor — both are downstream of this thread. The framework reference is [2026-05-26_multiscale-direct-h-step-v2]'s picture (C) ($\kappa_Q$ with relaxed factorization), which the thread tests by lifting the Gaussian commitment to the smallest non-trivial extension (Student-t at Q1) and then to mixture / non-parametric families at Q2A.

### The tree, the amendments, and the closure rule

Planned tree: $Q_1 \to \{Q_{2A}, Q_{2B}, Q_{2D}\}$ keyed by $Q_1$'s mechanical outcome on $\chi^2_{\text{marginal PIT}}$. Two amendments executed against the hash-chained `prior_tree_hash`:
- **A1** (`prior_tree_hash bea8f70c…`): Q1 SETTLED R-D mechanical / R-C substantive — 207 of 288 M2 climatology bins fitted $\nu$ at the upper boundary, a phase_a design hole (legitimate Gaussian-at-bin-level data, not estimator failure). A1 revised the R-D criterion text, closed Q2D, and rerouted to Q2A.
- **A2** (`prior_tree_hash 042f997c…`): Q2A SETTLED R-B2 — the R-B2 outcome category was pre-registered as a *mandatory thread amendment* trigger (not auto-advance), since Q2A was a leaf in the planned tree. A2 reopened Q2B.

Closure rule (Q2-level, both numeric and pre-registered): cumulative $\chi^2$-reduction share against gate-derived cuts. At Q2A, the band $\chi^2 \in (228.44, 2284.44]$ defines R-B2; at Q2B, the share is $\mathrm{share}_m = (\chi^2_{\text{base}} - \chi^2_{\text{after-fix}_m}) / (\chi^2_{\text{base}} - \chi^2_{\text{R-A2 cut}})$ with R-C3 firing when $\max_m \mathrm{share}_m < 0.15$. Q2B SETTLED R-C3 mandates *amendment OR closure*; closure was chosen, justified by the framework reframe in the resolution block.

### Post-mortem: what was bounded and what spawned

Three substantive findings exhaust the registered scope. **(i) The $\chi^2$ reduction curve asymptotes.** $M_0 \to M_1$ buys $16\times$ ($22{,}329 \to 1{,}371$); $M_1 \to M_3/M_4/M_5$ (mixture-2-G, mixture-3-G, KDE) buys only $1.44\times$ more ($1{,}371 \to 953.84$), with all three richer families $\chi^2$-indistinguishable (spread $13$ over $\sim 960$) and the simplest binding — KDE was the *worst* by $0.8$ units, exactly the proponent's `what_would_change_my_mind` #4. **(ii) Q2B mechanism ablations on $M_5$ fail or actively worsen.** Shares: $\mathrm{mean\_bias} = +0.109$ [$-0.39, +0.74$], $\mathrm{variance\_div} = -0.114$ [$-0.22, -0.01$], $\mathrm{seam} = -0.139$ [$-0.17, -0.09$] — all three below the $0.15$ null floor, two sign-determined NEGATIVE at $95\%$ CI. The SMC ensemble under-disperses at virtually every $(\text{day-type}, h)$ cell, so the one-sided variance cap is mis-specified at construction level; the seam mechanism is REGISTERED-NULL confirmed. Q2B was the second arbiter without mechanical/substantive gap; here substantive content runs *stronger* than mechanical — ablations did not merely fail, they degraded calibration. **(iii) The residual $\chi^2 \approx 954$ is non-distributional and $\sigma$-algebra-orthogonal to the natural conditioning.** Joint S9 + S10 is the applied confirmation of the identifiability obstruction parked at `~/Research/Mathematics/Resolvent_Framework/notes/unsorted/disintegration_diagnostic.md` (Bergna et al. 2026 Prop 1; Heckman–Singer 1984; Allahverdyan 2020): structured-noise and noisy-structure are observationally indistinguishable from $\kappa_Q$ alone. **The distributional-class lever is bounded.** Cross-link [2026-05-26_q1-student-t-vs-gaussian], [2026-05-26_q2a-richer-family-mixture-or-nonparametric], [2026-05-26_q2b-non-distributional-decomposition]; Q1's $D_{\mathrm{RATIO}_t} = 2.93$ also forces an asterisk on v2's headline magnitude (~$90\%$ kernel-family-conditional). Spawned [2026-05-27_resolution-paths-thread] (recorded in `resolution.spawned_threads`), which tests the $\sigma$-algebra-enrichment axis via candidate $Z$, stratified Patra–Sen, and synthetic Gaussian-DGP calibration — the three framework-prescribed resolution paths this thread's scope_limits deferred.
