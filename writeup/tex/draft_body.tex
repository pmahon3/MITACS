\maketitle

\begin{abstract}
A kernel forecaster that conditions only on a time-indexed
$\sigma$-algebra $Q$ estimates the conditional law $\kappa_Q$ of
a target observable. The kernel framing identifies the natural
levers for reducing residual structure in $\kappa_Q$: the
predictive-distribution family at fixed $Q$, refinement of $Q$
itself on time-derived axes, and the estimator's own sampling
variance. We instantiate a delay-embedding local-Gaussian
conditional-kernel estimator on Ontario electricity demand,
evaluate it as a pre-registered prediction experiment, and
measure the extent of each lever. A Student-$t$ kernel-family
lift over the Gaussian baseline reduces marginal-PIT $\chi^2$ by
$16\times$ (from $22{,}328$ to $1{,}371$); this is what univariate
data, processed under the kernel framing, naturally affords. The
remaining $\chi^2 \sim 1{,}371$ motivates a directed search through
the within-data lever space: five further pre-registered
experiments characterise where the residual structure lives.
Richer-than-Student-$t$ distributional families plateau
($1.44\times$ marginal return); a per-stratum Patra--Sen
diagnostic locates the residual on the \texttt{hour\_of\_week}
axis (range $\hat\alpha_L = 0.575$, with the hot zone the weekend
window and the cold zone Monday morning); single-axis
$\sigma$-algebra refinement on that axis shaves $0.18\%$ of the
leftover gap; joint two-axis refinement shaves $40\%$ at point
estimate with a paired-day bootstrap CI of $[+8, +484]$ $\chi^2$;
doubling the SMC particle count slightly widens that CI,
locating the wide CI in eval-window noise rather than estimator
sampling variance. Together the experiments direct the next move
to exogenous variables: the residual structure is cyclical and
social-weekly in shape, and the under-dispersion signature
(SMC ensemble variance below empirical at virtually every
$(h, \text{day-type})$ cell) points at missing exogenous content.
Hourly temperature is the prior-supported candidate; the
kernel framing accommodates it as a $\sigma$-algebra extension
$Q' = Q \vee \sigma(Z)$, and a registered forward experiment is
in place. All six experiments are hash-stamped at pre-registration
with production-code-path audits; results carry registry IDs and
body-SHA256 fingerprints throughout.
\end{abstract}

\section{Introduction}
\label{sec:intro}

A kernel forecaster that conditions on a time-indexed
$\sigma$-algebra $Q$ --- a delay embedding, calendar bins,
day-type labels --- estimates a conditional law $\kappa_Q$ of
the target observable from past samples. The framing identifies
three natural levers for reducing residual structure in $\kappa_Q$:
the predictive-distribution family at fixed $Q$, refinement of
$Q$ itself, and the sampling variance the estimator introduces.
It also identifies the boundary the levers stop at: when the
data-generating process carries information in some auxiliary
$Z \notin \sigma(Q)$, the residual structure within $\kappa_Q$
is observationally indistinguishable from intrinsic heavy-tailedness
on $\kappa_Q$ alone (Bergna et al.\ \texttt{arXiv:2605.06413},
2026, Prop.~1; Heckman--Singer, \emph{Econometrica}, 1984;
Allahverdyan, \texttt{arXiv:2002.07884}, 2020). Where the levers
stop and where the boundary sits are empirical questions; this
paper measures both.

We instantiate a delay-embedding local-Gaussian conditional-kernel
estimator on Ontario electricity demand
(Sections~\ref{sec:method}--\ref{sec:application}), evaluate it
as a pre-registered prediction experiment over a $500$-day
post-cutoff window, and run six further pre-registered experiments
along the three lever directions
(Sections~\ref{sec:univariate-extent}--\ref{sec:directed-search}).
Each experiment is hash-stamped at registration with production-
code-path audits; results carry paired-day bootstrap CIs and
verdicts against pre-registered cuts.

The paper has a three-act shape. Act~1
(Section~\ref{sec:univariate-extent}) measures the extent of the
predictive-distribution-family lever on univariate Ontario demand:
a Student-$t$ kernel-family lift over the Gaussian baseline
reduces marginal-PIT $\chi^2$ by $16\times$. This is what
univariate data, processed under the kernel framing, naturally
affords. Act~2 (Section~\ref{sec:directed-search}) is a directed
search through the remaining within-data lever space for where
the residual $\chi^2 \sim 1{,}371$ lives. Five further experiments
characterise the residual: richer distributional families plateau,
a per-stratum Patra--Sen diagnostic locates the residual on the
\texttt{hour\_of\_week} axis (range $\hat\alpha_L = 0.575$; hot
zone Fri 10:00 -- Sun 23:00; cold zone Mon 01:00 -- Mon 21:00),
single-axis $\sigma$-algebra refinement on the localised axis
shaves $0.18\%$ of the leftover gap, joint two-axis refinement
shaves $40\%$ with a wide CI, and doubling the SMC particle count
locates that wide CI in eval-window noise rather than estimator
sampling. Together the experiments \emph{direct} the next move:
the residual structure is cyclical and social-weekly, and the
under-dispersion signature points at missing exogenous content
beyond the time-indexed $\sigma$-algebra. Act~3
(Section~\ref{sec:auxiliary}) takes up where the directed search
points: auxiliary $Z$ is the framework-prescribed extension
($Q' = Q \vee \sigma(Z)$), hourly temperature is the
prior-supported candidate, and a registered forward experiment
against IESO published forecasts is the test in place.

\todo[inline]{Prior-work context, for the author group to supply; this
draft makes no comparative-novelty claims.}

\section{Theory and method}
\label{sec:method}

\subsection{Construction}
\label{sec:construction}

\paragraph{Embedding and library.}
Let \(s_t\) be a scalar observable (here, de-seasonalised hourly
demand; Section~\ref{sec:pipeline}) sampled on a regular grid
\(t \in \mathbb{Z}\). The delay embedding into \(\Rb^{d}\) is
\begin{equation}
\label{eq:embed}
x_t \;=\; (s_t,\; s_{t-1},\; \ldots,\; s_{t-(d-1)}) \,.
\end{equation}
Per day-type \(\mathrm{dt} \in \{\mathrm{weekday},
\mathrm{saturday}, \mathrm{sunday}\}\) the embedding dimension
\(d = d(\mathrm{dt})\) is selected on pre-cutoff library data by the
elbow rule on the one-step in-sample prediction-skill curve
\(\rho \mapsto d\). The pre-cutoff library is
\begin{equation}
\label{eq:library}
\mathcal{L} \;=\; \{\,(x_t, x_{t+1}) : t \le t_{\mathrm{cut}},\;
t \text{ on the day-type } \mathrm{dt}\,\} \,,
\end{equation}
with \(t_{\mathrm{cut}}\) the model-specification cutoff.

\paragraph{Local linear fit.}
For a query state \(x \in \Rb^{d}\), form the S-map kernel
weights~\cite{Sugihara1994}
\begin{equation}
\label{eq:weights}
w_i(x;\theta) \;=\;
\exp\!\left(-\theta\,\frac{\lVert x_i - x \rVert}{\bar d(x)}\right),
\qquad
\bar d(x) \;=\; \frac{1}{|\mathcal{L}|}\sum_{i \in \mathcal{L}}
\lVert x_i - x \rVert,
\qquad (x_i, y_i) \in \mathcal{L},
\end{equation}
with \(\theta \ge 0\) dimensionless: \(\theta = 0\) gives
\(w_i \equiv 1\) (uniform weights, the global linear fit) and
\(\theta\) increasing localises the weights toward the nearest
neighbours. \(y_i \equiv x_{t_i + 1}\). The
For each query state \(x\) and bandwidth \(\theta \ge 0\), the
weighted least-squares local linear one-step operator is
\begin{equation}
\label{eq:wls}
C(x;\theta) \;=\;
\arg\min_{C \in \Rb^{d \times d}}
\sum_{i \in \mathcal{L}} w_i(x;\theta) \, \lVert y_i - x_i C \rVert^{2}
\;=\; \bigl(X^{\top} W(\theta) X\bigr)^{-1} X^{\top} W(\theta) Y,
\end{equation}
with \(W(\theta) = \operatorname{diag}(w_i(x;\theta))\), \(X, Y\) the
stacked library pairs, and \(y_i \equiv x_{t_i + 1}\). Note that
\(C(x;\theta)\) is a \emph{family} of operators indexed by \(\theta\):
\(\theta = 0\) gives the global linear fit (uniform weights), and
\(\theta\) increasing localises the operator at \(x\). The one-step
map is then
\begin{equation}
\label{eq:drift}
x_{\text{next}} \;\approx\; x \cdot C(x;\theta) \qquad
\text{(row convention).}
\end{equation}

\paragraph{Bandwidth: a simplex-interpolated \(\theta\)-field.}
The bandwidth is not a free hyperparameter to be selected at the
query; it is itself a \emph{state-dependent quantity},
\(\theta = \theta(x)\), produced by a two-stage scheme.

\emph{Stage 1 (fit-time, one per library anchor).}
At each library point \(x_j \in \mathcal{L}\), select \(\theta^{*}_j\)
by true leave-one-out cross-validation on one-step squared error,
\begin{equation}
\label{eq:loocv}
\theta^{*}_j \;=\;
\arg\min_{\theta \ge 0}\;
\mathcal{R}_{\mathrm{LOO}}(x_j;\theta), \qquad
\mathcal{R}_{\mathrm{LOO}}(x_j;\theta)
\;=\; \frac{1}{|\mathcal{L}|}
\sum_{i \in \mathcal{L}}
\bigl\lVert y_i - x_i C^{(-i)}(x_j;\theta) \bigr\rVert^{2},
\end{equation}
with \(C^{(-i)}(x_j;\theta)\) the WLS fit at query \(x_j\) excluding
\((x_i, y_i)\). The collection
\(\{(x_j, \theta^{*}_j) : x_j \in \mathcal{L}\}\) is a discrete
\(\theta\)-field over the pre-cutoff embedding, frozen at fit time.

\emph{Stage 2 (query-time, per prediction).}
At a query state \(x\), interpolate the field via the
\((d+1)\)-simplex of nearest library neighbours,
\begin{equation}
\label{eq:simplextheta}
\theta(x) \;=\; \sum_{k=1}^{d+1} \pi_k(x)\, \theta^{*}_{j_k(x)},
\end{equation}
where \(\{j_k(x)\}_{k=1}^{d+1}\) are the indices of the \(d+1\) nearest
library points to \(x\), and \(\pi_k(x)\) are normalised Sugihara
simplex weights
\(\pi_k(x) \propto \exp(-\lVert x_{j_k(x)} - x\rVert / d_{\min}(x))\)
with \(d_{\min}(x) = \min_k \lVert x_{j_k(x)} - x\rVert\). The
field thus transports a state-dependent bandwidth from the library to
any query.

\emph{The operator at \(x\) is then a single evaluation of the
family,}
\begin{equation}
\label{eq:Cofx}
C(x) \;:=\; C\bigl(x;\,\theta(x)\bigr),
\end{equation}
which inherits the \(\theta\)-field's smoothness on the embedding.
The plain residual covariance at this fit,
\begin{equation}
\label{eq:diff}
\Sigma(x) \;=\; \frac{1}{|\mathcal{L}|}
\sum_{i \in \mathcal{L}} \bigl(r_i - \bar r\bigr)
\bigl(r_i - \bar r\bigr)^{\!\top},
\qquad r_i := y_i - x_i C(x),
\quad \bar r := \tfrac{1}{|\mathcal{L}|}\sum_i r_i,
\end{equation}
defines the diffusion. Together they specify the state-dependent
Gaussian Markov kernel
\begin{equation}
\label{eq:kernel}
\Pi(x, \cdot) \;=\; \Nrm\!\bigl(x \, C(x),\; \Sigma(x)\bigr).
\end{equation}

\paragraph{Why the plain (unweighted) covariance.}
A weighted residual covariance \(\Sigma^{w}(x) = \sum_i w_i\,
(r_i - \bar r)(r_i - \bar r)^{\!\top} / \sum_i w_i\) shrinks toward
the small-residual core as \(\theta(x)\) grows --- a collapse
pathology the kernel-weighting machinery induces but does not
represent in the estimand. The plain covariance is the unbiased local
second moment of the residuals selected into the fit by \(w\), free
of that shrinkage.

\paragraph{The conditional law \(\kQ\) the estimator targets.}
On a probability space \((\Omega, \mathcal{F}, P)\) with two
observations \(Q, F\), the Rokhlin disintegration
theorem~\cite{Rokhlin1952,Bogachev2007} produces a Markov kernel
\begin{equation}
\label{eq:kappaQ}
\kQ(q, \cdot) \;=\; P(F \in \cdot \mid Q = q),
\end{equation}
determined by the measure --- not posited as an additional dynamical
law. Under temporal indexing of the observations, finite-dimensional
distributions are coherent, so the kernels compose: the
Chapman--Kolmogorov equation is a consequence, not a hypothesis, and
yields a Markov semigroup \(\{\Pi_t\}\)~\cite{Eisner2015}. The
companion Koopman--Perron duality
\begin{equation}
\label{eq:KP}
\int K_t\, g\, \dd\mu \;=\; \int g\, \dd(P_t^{*}\mu)
\end{equation}
relates the Koopman operator \(K_t g = g \circ T_t\) (when the
dynamics is deterministic, recovering the classical Koopman operator
when the kernels are Dirac~\cite{LasotaMackey1994}) to its
Perron--Frobenius adjoint \(P_t^{*}\) on densities. Three layers are
useful to keep notationally distinct: \(\kQ\) (the disintegration
kernel; a conditional law), \(\{\Pi_t\}\) (the time-indexed Markov
semigroup), and \(K_t\) (the operator-layer object).

\paragraph{The estimator's relationship to \(\kQ\).}
The \(\{\Pi_t\}\) semigroup and the operator family
\(\{K_t\} / \{P_t^*\}\) exist in the data measure by the construction
of Section~\ref{sec:multistep}'s framework reference: stationarity of
the de-seasonalised series and the delay-embedding observation
structure together satisfy the temporal coherence that forces the
Chapman--Kolmogorov property; the disintegration kernel \(\kQ\)
admits the family, derived from the measure rather than posited.
The object Section~\ref{sec:construction} constructs is a
finite-sample, parametric, Gaussian-form estimator of the
\emph{single-step slice} \(\Pidelta\) of that family at the
delay-embedding layer. From the estimated \((C(x), \Sigma(x))\) the
operator-layer slice \(K_{\Delta} / P_{\Delta}^{*}\) is recoverable
in the non-Dirac regime (\(\Sj \neq 0\)).
\(\Cj\) is a linearisation of the conditional-mean factor; \(\Sj\) is
the second moment of \(\kQ\)'s Gaussian projection; \(\Sj \to 0\)
reproduces the classical-Koopman (deterministic) collapse. Three
qualifiers bound this correspondence:

\begin{enumerate}
  \item \textbf{Register.} \(\kQ\) is determined by the measure; this
  work \emph{constructs} a Gaussian proxy and fits it from finite
  data --- ``estimator of,'' not ``instance of.''

  \item \textbf{Imposed Gaussianity.} \(\Pij = \Nrm(x \Cj, \Sj)\)
  coincides with \(\kQ\) only where the local conditional law is
  itself Gaussian. Section~\ref{sec:caveat} reports the
  rank-1 structural consequence and the measured non-Gaussianity of
  the scalar coord-0 innovation.

  \item \textbf{Scope of extraction.} What is extracted is the
  single-step slice \(\Pidelta\); other slices
  \(\Pi_{h\Delta}\) of the same data-measure semigroup
  \(\{\Pi_t\}\) are not estimated directly. The multi-step
  propagation modes of Section~\ref{sec:multistep} extend
  \(\widehat{\Pidelta}\) to \(h\) steps by iteration; whether the
  iterate \((\widehat{\Pidelta})^{h}\) tracks the directly-extractable
  \(\widehat{\Pi}_{h\Delta}\) is a separable empirical question
  testable by a Chapman--Kolmogorov consistency check at one or more
  discrete horizons. The check is reported as a synthetic gate in
  Section~\ref{sec:synth} and as an in-sample Ontario probe in
  Section~\ref{sec:limits}. Existence of \(\{\Pi_t\}\) is a property
  of the measure; access to it beyond \(\Pidelta\) is a property of
  the extraction scheme.
\end{enumerate}

\subsection{Multi-step propagation: mean iteration and SMC}
\label{sec:multistep}

The one-step kernel \(\Pij\) admits two distinct \(h\)-step
propagation modes. Both \emph{iterate} the estimated single-step
\(\widehat{\Pidelta}\) to extend it to \(h\) steps. The data-measure
semigroup \(\{\Pi_t\}\) and its slices \(\Pi_{h\Delta}\) at horizons
\(h > 1\) exist in the measure (see qualifier~3 above and
Section~\ref{sec:construction}); whether the iterate
\((\widehat{\Pidelta})^{h}\) agrees with the directly-extractable
slice \(\widehat{\Pi}_{h\Delta}\) is an empirical
Chapman--Kolmogorov-consistency question, separable from the
iteration scheme itself.

\emph{Mean iteration} (point trajectory). Starting from
\(x_0\), produce a deterministic length-\(h\) trajectory by iterating
the conditional mean,
\begin{equation}
\label{eq:meaniter}
x_h^{\mathrm{mean}} \;=\;
x_{h-1}^{\mathrm{mean}} \cdot C\!\bigl(x_{h-1}^{\mathrm{mean}}\bigr),
\qquad h = 1, 2, \ldots
\end{equation}
The diffusion \(\Sj\) sizes a per-step interval but never feeds
forward; the point forecast at horizon \(h\) is
\(\hat{x}_h = x_h^{\mathrm{mean}}\).

\emph{Sequential Monte Carlo}~\cite{DoucetJohansen2009}
(ensemble of trajectories). Maintain \(M\) particles
\(\{x_h^{(m)}\}_{m=1}^{M}\); at each step, draw each particle's next
state from the local kernel at its current position,
\begin{equation}
\label{eq:smc-step}
x_h^{(m)} \;\sim\; \Pi\!\bigl(x_{h-1}^{(m)}, \cdot\bigr)
\;=\; \Nrm\!\bigl(x_{h-1}^{(m)} \cdot C\!\bigl(x_{h-1}^{(m)}\bigr),\;
\Sigma\!\bigl(x_{h-1}^{(m)}\bigr)\bigr).
\end{equation}
The Gaussian form of Eq.~\ref{eq:smc-step} samples from the
estimator's Gaussian projection of \(\kQ\); an alternative
\emph{empirical-residual} variant resamples a coordinate-0 residual
\(r^{(m)}\) directly from the local fit's residual array and sets
\(x_h^{(m)} = x_{h-1}^{(m)} \cdot C(x_{h-1}^{(m)}) + r^{(m)}\)
(rank-1: only coord 0 is stochastic; coords 1..d-1 are deterministic
lag shifts). Both variants iterate \(\Pidelta\); they differ only in
which sampling law the per-step innovation is drawn from.
Point forecast at horizon \(h\) is the ensemble mean
\(\hat{x}_h = M^{-1}\sum_m x_h^{(m)}\); predictive intervals are
ensemble quantiles.

The two modes differ in their use of the diffusion factor \(\Sj\).
Mean iteration discards \(\Sj\) at each step; SMC propagates it
(or, in the empirical variant, the residuals it summarises).
Qualifier 2 (imposed Gaussianity; see Section~\ref{sec:caveat})
therefore has asymmetric consequences across the two modes: a
structurally heavy-tailed innovation enters the mean-iteration point
forecast only through its effect on \(C\), but enters the SMC
predictive distribution directly at every step.

\section{Illustrative simulations}
\label{sec:illustrations}

\todo[inline]{Placeholder for a synthetic-system illustration of the
two propagation modes of Section~\ref{sec:multistep}, to be added
once the design is finalised.}

\section{Structural facts about the estimator}
\label{sec:caveat}

\paragraph{\(\Sj\) is rank-1 by construction.} For a single-variable
delay embedding, coordinates \(2, \ldots, d\) of the one-step image
are deterministic shifts of the input coordinates
(\(Y_{[:,\,1:]} = X_{[:,\,:-1]}\) exactly). The only stochastic
content of the one-step map is the scalar coordinate-0 innovation;
\(\Sj\) is therefore rank-1 by construction, independent of data.

\paragraph{The scalar innovation is heavy-tailed.} The
non-Gaussianity question reduces to a univariate one in the
coordinate-0 innovation. On the Ontario library this innovation
carries \(\mathrm{kurt}_{\mathrm{exc}} \approx 24\)--\(33\)
(per-anchor, one-step, no propagation). The estimator's
non-Gaussianity diagnostic, validated against synthetic ground truth
(Section~\ref{sec:synth}), discriminates this heavy-tailed law from
the Gaussian reference.

\section{Application and results}
\label{sec:application}

\subsection{Ontario pipeline}
\label{sec:pipeline}

The instantiation is a four-stage pipeline on IESO public
\texttt{PUB\_Demand}: hourly demand is de-seasonalised to a stationary
\(z\)-score (Section~\ref{sec:zscore}); each hour is tagged
\(\{\mathrm{weekday}, \mathrm{saturday}, \mathrm{sunday}\}\) on a
single configured day-anchor (matching IESO's HE1--HE24 delivery-day
convention); an embedding dimension is selected per day-type by an
elbow rule on the prediction-skill-vs-dimension curve; and the
Section~\ref{sec:method} estimator is fit per anchor. The delivery-day
window and the day-type clock share a single source of truth, so
multi-step trajectories do not cross an un-modelled day-type
seam.\footnote{Three calendar days are excluded at load time as
non-representative of normal demand dynamics: 14--16 August 2003, the
Northeast blackout and its grid-restoration window.}

\subsection{The \(z\)-score transform is a modelling choice, not a
preprocessing detail}
\label{sec:zscore}

De-seasonalisation is
\begin{equation}
\label{eq:zscore}
z \;=\; \frac{D - \mu_{m,h}}{\sigma_{m,h}},
\end{equation}
with \((\mu_{m,h}, \sigma_{m,h})\) a month \(\times\) hour-of-day
climatology estimated strictly from pre-cutoff data. This one
climatology object enters all four estimator layers --- the embedded
state, the drift fit, the diffusion (\(\Sj\) is the covariance of
\(z\)-residuals), and the demand-space round-trip --- so it is an
implicit seasonal model coupled into \(\kQ\), not a benign
preprocessing step, and two questions about its specification were
investigated on the development-set window.

The phase-asymmetry structure of Section~\ref{sec:caveat} is not a
de-seasonalisation artefact: in raw-demand units, the phase
correlation \(\mathrm{corr}(\dd D/\dd t,\, e)\) is \(-0.247\),
\(-0.250\), \(-0.217\) under the global, month\,\(\times\)\,hour, and
no-transform specifications --- near-identical, and already present in
raw demand. The transform does, however, earn its place on accuracy:
re-fitting the pipeline end-to-end gives
\begin{equation}
\label{eq:zscore-mape}
\MAPE \;=\; 3.86\%\ \text{(full)}
\;\to\; 11.98\%\ \text{(scale-only, }z = D/\sigma_{m,h})
\;\to\; 13.57\%\ \text{(raw demand)}.
\end{equation}
Both the centring and the scaling sub-steps are load-bearing; the
month\,\(\times\)\,hour climatology is retained.

\subsection{Out-of-sample backtest}
\label{sec:backtest}

The predictor is backtested on Ontario ground truth for
\(|\mathcal{D}| = 500\) complete delivery days strictly after the
model-specification cutoff; fits and \(z\)-score climatology use only
\(\le\)-cutoff data, and post-cutoff actuals enter only as query
history and scoring truth. With forecast \(\hat{y}_{d,h}\) and actual
\(y_{d,h}\) over delivery days \(d \in \mathcal{D}\) and horizons
\(h \in \{1, \ldots, 24\}\),
\begin{equation}
\label{eq:mae-mape}
\MAE \;=\; \frac{1}{24|\mathcal{D}|}
\sum_{d \in \mathcal{D}} \sum_{h=1}^{24}
\bigl| \hat{y}_{d,h} - y_{d,h} \bigr|,
\qquad
\MAPE \;=\; \frac{1}{24|\mathcal{D}|}
\sum_{d \in \mathcal{D}} \sum_{h=1}^{24}
\frac{\bigl| \hat{y}_{d,h} - y_{d,h} \bigr|}{y_{d,h}} \,.
\end{equation}
Section~\ref{sec:multistep} gives two propagation modes (mean
iteration and SMC); each is run under two bandwidth choices ---
production state-dependent \(\theta(x)\) via Eq.~\ref{eq:simplextheta}
and global-OLS \(\theta = 0\) --- and SMC under both Gaussian
(Eq.~\ref{eq:smc-step}) and empirical-residual innovation sampling.
The full \(\{\text{propagation}\} \times \{\theta\} \times
\{\text{innovation law}\}\) grid gives six predictors,
labelled 1--6 along the propagation \(\times\)
\(\theta\) \(\times\) innovation grid as listed in
Table~\ref{tab:predictors}. Point error follows a diurnal envelope
across all six predictors (Figure~\ref{fig:diurnal} shows the
underlying demand distribution; Figure~\ref{fig:per-horizon} the
predictor differences). The lowest \(\MAE\) is predictor 2
(mean-iteration, \(\theta = 0\); \(751.6\)~MW), the highest is
predictor 1 (\(768.8\)~MW), and all six lie within a \(17\)-MW band
over the full \(500\)-day population. The two
state-dependent-\(\theta(x)\) predictors (1 and 5) read above their
\(\theta = 0\) counterparts by a few to a few-dozen MW at most
horizons. Interval coverage (Section~\ref{sec:distributional})
spans a much wider range than the point error: the mean-iteration
predictors cover \(\approx 21\%\) of actuals against a nominal
\(\approx 68.3\%\) one-sigma band, the SMC predictors
\(68\)--\(72\%\).

\begin{table}[h]
\centering
\small
\caption{The six predictors of Section~\ref{sec:backtest}.
``\(\theta = 0\)'' is the global linear fit (uniform weights);
``\(\theta(x)\)'' is the simplex-interpolated state-dependent
bandwidth (Eq.~\ref{eq:simplextheta}). SMC predictors use \(M = 200\)
particles; the per-step innovation is drawn either from the local
Gaussian \(\Nrm(0, \Sj)\) (Eq.~\ref{eq:smc-step}) or by resampling
the coordinate-0 residual array (the empirical variant).}
\label{tab:predictors}
\begin{tabular}{l l l l}
\toprule
\# & propagation       & \(\theta\)       & innovation law \\
\midrule
1  & mean-iteration    & \(\theta(x)\)    & --- \\
2  & mean-iteration    & \(\theta = 0\)   & --- \\
3  & SMC               & \(\theta = 0\)   & Gaussian       \\
4  & SMC               & \(\theta = 0\)   & empirical      \\
5  & SMC               & \(\theta(x)\)    & Gaussian       \\
6  & SMC               & \(\theta(x)\)    & empirical      \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[h]
\centering
\includegraphics[width=\linewidth]{fig_diurnal_envelope.pdf}
\caption{Hourly Ontario demand distribution by day-type over the
\(|\mathcal{D}| = 500\)-day population, on the HE1--HE24 delivery
clock (HE\,\(k\) is the hour ending at \(k{:}00\)). The diurnal
envelope is a property of the demand process: weekday carries the
sharpest morning ramp and the tallest evening peak, saturday a
gentler ramp, sunday the smallest amplitude and latest peak.}
\label{fig:diurnal}
\end{figure}

\begin{figure}[h]
\centering
\includegraphics[width=\linewidth]{fig_per_horizon_mae.pdf}
\caption{Per-horizon \(\Delta\MAE\) of the five non-baseline
predictors against predictor 2 (mean-iteration, \(\theta = 0\)),
the lowest-MAE predictor in the grid. Absolute MAE itself is
dominated by the diurnal envelope of Figure~\ref{fig:diurnal} and
obscures the predictor-to-predictor separation. The
state-dependent-\(\theta(x)\) predictors (1, 5) consistently sit
above predictor 2 by a few to a few-dozen MW; predictors 3, 4, 6
sit within \(\pm 10\)~MW of predictor 2 at most horizons. The
predictor-to-predictor spread is small relative to the diurnal
envelope of MAE (Appendix~\ref{app:backtest}).}
\label{fig:per-horizon}
\end{figure}

\subsection{Distributional diagnostics on the SMC predictive law}
\label{sec:distributional}

The full ensemble at each \((d, h)\) of the SMC predictors 3--6 of
Section~\ref{sec:backtest} carries information the point/interval
summary does not surface. Three diagnostics are reported below as
observations; their interpretation is deferred to follow-up analysis.

\emph{Multi-sigma coverage} (Figure~\ref{fig:multisigma}). At
nominal central levels
\(\alpha \in \{0.50, 0.683, 0.80, 0.90, 0.95, 0.99\}\), the
empirical coverage of the corresponding sample-quantile band, pooled
over horizons and day-types, reads on the diagonal within
\(\approx 1\) percentage point at every level for the two
empirical-residual predictors (4 and 6); the two Gaussian-SMC
predictors (3 and 5) read above the diagonal at small levels and
below it at large levels.

\begin{figure}[h]
\centering
\includegraphics[width=\linewidth]{fig_multisigma_coverage.pdf}
\caption{Empirical vs.\ nominal coverage at six central levels for
the four SMC predictors, pooled over horizons and day-types
(\(|\mathcal{D}| = 500\)). Diagonal is perfect calibration.}
\label{fig:multisigma}
\end{figure}

\emph{PIT shape} (Figure~\ref{fig:pit}). The probability-integral
transform of the actual through the per-step predictive
distribution, pooled across horizons and day-types, reads
right-decreasing across all four SMC predictors: actuals fall in
the lower half of the predictive more often than uniform
expectation. The shape persists across the propagation
\(\times\) innovation-law grid.

\emph{Ensemble shape and CRPS.} At \(h = 1\) the per-anchor sample
excess-kurtosis averaged across the population reads
\(\overline{\mathrm{kurt}_{\mathrm{exc}}} \approx +4.8\) for the
empirical-residual predictors (4 and 6) and \(\approx -0.04\) for
the Gaussian predictors (3 and 5). The sample-form CRPS (with the
\(M/(M-1)\) unbiased correction), pooled over all
\((d, h, \mathrm{daytype})\), reads predictor~3 (\(539.2\)) \(<\)
4 (\(542.2\)) \(<\) 5 (\(547.6\)) \(<\) 6 (\(550.7\))~MW.

\begin{figure}[h]
\centering
\includegraphics[width=0.85\linewidth]{fig_pit_panel.pdf}
\caption{Probability-integral-transform histograms for the four SMC
predictors, pooled over the full population. Dotted line: the
uniform density of perfect calibration.}
\label{fig:pit}
\end{figure}

\begin{figure}[h]
\centering
\includegraphics[width=\linewidth]{fig_pit_by_horizon.pdf}
\caption{The marginal-PIT pathology grows sharply with propagation
horizon. At $h = 1$ the four SMC predictors all read approximately
uniform with mild over-coverage of the lower tail; by $h = 12$ the
distribution has collapsed to a pile-up at $\text{PIT} = 0$ and a
right-decreasing tail across all four predictors, signalling that
actuals fall below the predictive's lower quantiles far more often
than the uniform expectation. The Gaussian kernel family is
already strained at $h = 1$ and structurally inadequate at iterated
horizons. Section~\ref{sec:univariate-extent} measures how much of
this pathology the Student-$t$ kernel-family extension recovers at
the marginal-PIT level (the conventional single-horizon test;
pooled over $h$).}
\label{fig:pit-by-horizon}
\end{figure}

\subsection{Validation against synthetic ground truth}
\label{sec:synth}

Three gates, summarised in Table~\ref{tab:gates}, all pass on
known-ground-truth synthetic systems.

\begin{table}[h]
\centering
\small
\caption{Synthetic-ground-truth validation gates. Recovery and
non-Gaussianity are recorded as a single CLAIM-grade artifact
(body sha256 \texttt{d8435415\(\ldots\)}); the Chapman--Kolmogorov
consistency gate is INSPECTION-only.}
\label{tab:gates}
\begin{tabular}{l l p{0.42\linewidth}}
\toprule
Gate & System & Result \\
\midrule
Recovery
  & Known-\((A,Q)\) VAR(1)
  & drift rel.\ err.\ \(0.048\) (tol.\ \(0.10\)), diffusion
    \(0.026\) (tol.\ \(0.25\)) \\
Non-Gaussianity
  & Gaussian / \(t_5\) VAR(1)
  & excess kurt.\ \(-0.05\) / \(+4.72\); heavy tail flagged \\
CK consistency
  & VAR(1), \(\Pi_{h\Delta}\) direct vs.\ \((\Pidelta)^{h}\) iterate
  & \(h \in \{2,4,8,12,24\}\): drift rel.\ err.\
    \(0.04\)--\(0.09\), diffusion \(0.03\)--\(0.06\) relative ---
    PASSES at all five horizons \\
\bottomrule
\end{tabular}
\end{table}

\noindent
The three gates confirm, on systems with known ground truth, that
\((\Cj, \Sj)\) are recovered within tolerance, that the
non-Gaussianity diagnostic discriminates a Gaussian from a \(t_5\)
innovation, and that the iterated single-step kernel is a faithful
proxy for the directly-fit \(h\)-step kernel when the underlying
dynamics is genuinely VAR(1). The CK gate fits
\(\widehat{\Pi}_{h\Delta}\) by running the production
\texttt{\_local\_fit\_at} on the lagged pair \((x_t, x_{t+h})\) ---
the same estimator used for the single step --- and compares against
the closed-form \((A^{h},\, \sum_{k=0}^{h-1}(A^{k})^{\top}Q A^{k})\)
of the generative process; the iterated and direct fits agree within
\(\le 6\%\) relative-Frobenius diffusion error at every \(h\) tested.
Whether the same agreement holds on Ontario is examined separately as
an inspection-only probe (Section~\ref{sec:limits}).

\section{What univariate data gives you under the kernel framing}
\label{sec:univariate-extent}

The kernel framing of Section~\ref{sec:method} identifies the
predictive-distribution family at fixed conditioning $\sigma$-algebra
as one of the natural levers for reducing residual structure in
$\kappa_Q$. The Gaussian instantiation of
Section~\ref{sec:construction} is the simplest choice on that
lever; the diagnostics of Section~\ref{sec:distributional} show
the Gaussian SMC predictors over-cover small intervals and
under-cover large ones, and the marginal PIT is sharply
right-decreasing. Both observations point at the kernel family
itself --- not its conditioning structure --- as the binding
constraint at this level of generality. The natural question
under the framing is then how much of the marginal-PIT residual
the heavy-tailed extension of the family resolves.

This was registered as the root experiment (Q1, 2026-05-26) of
the distributional-class thread. The two M-models are nested: M1
replaces the Gaussian kernel with a Student-$t$ kernel at one
global $\nu$ per day-type and keeps the seasonal climatology of
\S\ref{sec:zscore}; M2 additionally refits the climatology under
Student-$t$ MLE per $(\text{month}, \text{hour-of-day})$. With
$1000$-paired-day bootstrap CIs:

\begin{center}
\begin{tabular}{lrrr}
\toprule
                                       & point      & CI low     & CI high   \\
\midrule
$\chi^{2}_{M0}$ (Gaussian baseline)    & $22{,}328$ & $19{,}028$ & $25{,}731$ \\
$\chi^{2}_{M1}$ (Student-$t$ kernel)   & $1{,}371$  & $1{,}069$  & $1{,}817$  \\
$\chi^{2}_{M2}$ (full Student-$t$)     & $1{,}754$  & $1{,}383$  & $2{,}267$  \\
\bottomrule
\end{tabular}
\end{center}

The lift is large and asymmetric. M1's $16\times$ reduction over
M0 is the headline; M2's regression vs.\ M1 is the
diagnostic.\footnote{Registry entry: Q1 =
\path{2026-05-26_q1-student-t-vs-gaussian} (result.yaml body-SHA256
\texttt{1e29cf47\ldots}; INSPECTION-only; the mechanical R-D
verdict fired on the M2 climatology MLE saturating $\nu \to \infty$
on $\sim 700$-sample bins where the CLT-aggregated marginal demand
is approximately Gaussian. The substantive verdict is R-C per
$\chi^{2}_{M1} = 1{,}371 > 500$; the heavy-tailed kernel matters,
but the marginal residual is not absorbed by it alone.)} Empirical
$\hat\nu$ per day-type sits in $\{4.19, 4.62, 4.61\}$, within
$0.4$ of the in-sample-kurtosis prediction $\nu \approx 4.2$--$4.25$
from the structural excess-kurtosis of Section~\ref{sec:caveat}.
The heavy tail lives in the $\hat\kappa_1$ innovation on recent
state, not in the seasonal climatology: M1 outperforms M2 because
the bin-level marginal demand at $(\text{month}, \text{hour})$ is
already CLT-flattened over $\sim 690$ weather samples per bin,
while the conditional residual on recent state remains heavy-tailed.

This is what the kernel framing, restricted to the
predictive-distribution-family lever on univariate data, extracts:
a $16\times$ marginal-$\chi^{2}$ reduction and a clean per-day-type
$\hat\nu$ estimate consistent with the structural prediction.
The remaining $\chi^{2} \sim 1{,}371$ is not within the
distributional family at fixed $Q$ --- it is what the M1$\to$M2
diagnostic, the per-cell distribution of M1's PIT residuals, and
the structural under-dispersion of the SMC ensemble all point at
as the next question. Section~\ref{sec:directed-search} takes that
question to a directed search across the remaining within-data
levers the framing identifies.

\section{Mapping the residual: a directed search through the
within-data lever space}
\label{sec:directed-search}

The kernel framing identifies two more within-data levers beyond
the predictive-distribution family at fixed $Q$: further refinement
of the $Q$-conditioning itself (extending $\sigma(Q)$ by adding
time-derived strata), and the sampling variance the SMC estimator
introduces in propagating $\widehat\Pi_\Delta$. Five further
pre-registered experiments traverse both. Each opens with a
question the prior result raised, runs production-code-path code
against pre-registered numeric cuts, and closes by handing the
next question to the next experiment. Each carries a phase~A
artifact (numeric falsification and corroboration cuts; proponent
and devil's-advocate point forecasts stamped simultaneously before
any data is touched), a code-path audit, a phase-fidelity check on
both the script and its result, and an arbiter verdict; results
are stamped via \verb|experiment.provenance.make_result()| with
body-SHA256 fingerprints, and the registry chain
\verb|verify|-resolves clean at the time of writing. The five
experiments are reported below in their registered chronological
order; Figure~\ref{fig:act2-forest} summarises where each lands
against its pre-registered cuts.

\begin{figure}[h]
\centering
\includegraphics[width=\linewidth]{fig_act2_forest.pdf}
\caption{The directed search bounds the within-data lever space
inside R-B. Each experiment is reported with $95\%$ paired-day
bootstrap CI bars on its primary marginal-PIT $\chi^2$ statistic;
background shading marks the pre-registered R-A corroboration
(green, $\le 228$), R-B inconclusive band (grey,
$(228, 2284]$), and R-C falsification (red, $> 2284$) thresholds
inherited from Q2A's gate-derived cuts. \emph{All six experiments
fall in R-B} --- none clears the corroboration target, none
triggers falsification. Q1's M1 Student-$t$ kernel
(top, $\chi^2 = 1371.6$) is the post-Act-1 starting point, $16\times$
below Q1's Gaussian baseline M0 ($\chi^2 = 22{,}328.6$;
not plotted, would dominate the axis). The directed search reduces
this further only inside R-B: Q1B and Q1B$'$ at $\chi^2 \approx 670$
are the lowest the within-data lever space reaches. Q1A and Q2A
co-locate within $1.3\,\chi^2$ at the seed-equality reproduction
point (cross-experiment audit-chain corroboration). P1's per-stratum
$\hat\alpha_L$ diagnostic is on a different scale ($\alpha_L \in
[0, 1]$; binding axis \texttt{hour\_of\_week} with
$\mathrm{range}(\hat\alpha_L) = 0.575$) and is not on this forest;
its role is to \emph{locate} the residual structure that the
$\chi^2$-side experiments here probe.}
\label{fig:act2-forest}
\end{figure}

\paragraph{Q2A (2026-05-26): does the residual live in the
distributional family beyond Student-$t$?} Q1's $16\times$ lift
left $\chi^{2}_{M1} = 1{,}371$ unabsorbed at the
predictive-distribution-family lever. Q2A tested three
richer-than-Student-$t$ families at the same $Q$: a two-component
Gaussian mixture (M3), a three-component Gaussian mixture (M4),
and a kernel-density estimator (M5). All three are
$\chi^{2}$-indistinguishable at $\chi^{2}_{20} \approx 954$
(the spread across the three is $\sim 13$ $\chi^{2}$, smaller than
the bootstrap noise on any one of them; M5 is the worst by
$0.8$). The marginal-return curve asymptotes: M0$\to$M1 $=
16\times$, M1$\to$best $= 1.44\times$. The family-at-fixed-$Q$
lever is at capacity; the residual $\chi^{2} \sim 954$ is not in
the predictive-distribution family. Q2A also surfaced a
substantive sub-finding: Saturday's per-stratum $\chi^{2}$ is
$\sim 4\times$ weekday's at every family tested, family-invariant.
This points the next question outward: if the residual is not
distributional, can a same-$Q$ mechanism (mean-bias correction,
variance cap, seam exclusion) account for it?\footnote{Registry:
Q2A = \path{2026-05-26_q2a-richer-family-mixture-or-nonparametric}
(\texttt{6ae6ce53\ldots}; CLAIM).}

\begin{figure}[h]
\centering
\includegraphics[width=\linewidth]{fig_signed_error_by_hour.pdf}
\caption{Saturday's $4\times$-weekday $\chi^{2}$ asymmetry,
family-invariant in Q2A, is structural and visible at the level of
signed forecast error by hour-of-delivery-day. Weekday error peaks
near $+800~\mathrm{MW}$ at the midday plateau (HE9--HE12);
Saturday's morning-ramp error reaches $\approx +1800~\mathrm{MW}$
at HE8--HE9 and remains above $+800$ through evening; Sunday
peaks largest at $\approx +1700~\mathrm{MW}$ at HE8--HE9, again
sustained through the day. Both weekend day-types show the
forecast running systematically high through the daytime
window; weekday is broadly closer to balanced. The asymmetry is
\emph{independent} of distributional family --- Q2A confirmed
M3, M4, M5 are $\chi^{2}$-indistinguishable per day-type --- so
the gap lives in the conditioning structure, not in the
predictive-distribution family.}
\label{fig:signed-error-by-hour}
\end{figure}

\paragraph{Q2B (2026-05-26): does the residual live in a
same-$Q$ mechanism the framing has not yet absorbed?} Q2A bounded
the family lever and the Saturday-vs-weekday asymmetry it
revealed pointed at structural rather than distributional
explanations. Q2B was the registered ablation experiment: three
pre-registered mechanisms (\texttt{mean\_bias} adds the empirical
per-$(h, \text{day-type})$ mean bias before marginal PIT;
\texttt{variance\_div} caps one-sided iterated over-dispersion;
\texttt{seam} restricts PIT aggregation to exclude seam-domain
rows). The verdict statistic is the explained share of the gap
between M3 baseline and the R-A2 corroboration cut. Point shares
landed at $(\texttt{mean\_bias} +0.109, \texttt{variance\_div}
-0.114, \texttt{seam} -0.139)$ with two of three sign-determined
negative at $95\%$ CI. The ablations did not just fail to improve
calibration; two of three actively degraded it. The
\texttt{variance\_div} sign-flip is itself a substantive finding:
$\sigma^{2}_{\text{iter}} < \sigma^{2}_{\text{empirical}}$ at
virtually every $(h, \text{day-type})$ cell, so the one-sided cap
designed for over-dispersion is wrong-directional in an
under-dispersion regime. \textbf{Under-dispersion of an iterated
forecast is the canonical signature of missing exogenous content}
--- the predictor's predictive distribution is too narrow because
the conditioning $\sigma$-algebra is too coarse. The next question
this raises is sharp: \emph{which} content is missing, and where
in the time-indexed $\sigma$-algebra is it concentrated?\footnote{Registry:
Q2B = \path{2026-05-26_q2b-non-distributional-decomposition}
(\texttt{68d156a8\ldots}; CLAIM).}

\paragraph{P1 (2026-05-27): where in the time-indexed
$\sigma$-algebra is the residual concentrated?} Q2B's
under-dispersion signature pointed at missing exogenous content
but did not localise it. P1 is the diagnostic applied to do that:
the Patra--Sen (2016) two-component non-Gaussian mixture estimator
$\hat\alpha_L$ applied per stratum to Q1's M1 PIT residuals
across five candidate conditioning axes (\texttt{hour\_of\_week},
\texttt{time\_of\_day}, \texttt{season}, \texttt{day\_type},
\texttt{demand\_quantile}). If $\hat\alpha_L$ varies materially
across strata on any axis, that axis is where the missing
structure concentrates. The verdict statistic
$\max_{\text{axis}} \text{range}(\hat\alpha_L) = 0.575$
$[0.4475, 0.6475]$ clears the R-A cut $0.20$ by more than $2\times$
at the lower CI bound, with the binding axis
\texttt{hour\_of\_week}. The hot zone is the weekend window
(Fri 10:00 -- Sun 23:00, $\hat\alpha_L \in [0.55, 0.61]$); the
cold zone is Monday morning ($\hat\alpha_L = 0.035$, essentially
Gaussian). \textbf{The missing structure is cyclical and
social-weekly}, not seasonal or climatological --- season ranks
fourth of five axes at $0.135$, barely above the R-A cut. The
day-type axis ($0.345$, with Saturday $0.6475 >$ Sunday $0.4850 >$
Weekday $0.3025$) independently corroborates Q2A's
Saturday-$4\times$ asymmetry. P1 localises the structure but
does not absorb it. The next question hands to a
$\chi^{2}$-side test: does refining the $\sigma$-algebra on the
binding axis close the marginal-PIT gap?\footnote{Registry: P1 =
\path{2026-05-27_p1-patra-sen-per-stratum-localization} (arbiter
body-SHA256 \texttt{5eb024b7\ldots}; no standalone result.yaml,
verdict stamped in arbiter).}

\begin{figure}[h]
\centering
\includegraphics[width=\linewidth]{fig_bias_climatology.pdf}
\caption{P1's per-stratum $\hat\alpha_L$ ranking
(Saturday $0.65 >$ Sunday $0.49 >$ Weekday $0.30$) corresponds
visually to the structure of the climatology bias
$\mu_{m,h} - y$. Weekday is mostly negative with a mild midday
lift to $\approx -250~\mathrm{MW}$; Saturday and Sunday carry
positive morning-ramp biases reaching $+750~\mathrm{MW}$ and
$+1100~\mathrm{MW}$ respectively. The day-type axis carries
genuine localising signal that P1 measures as a non-Gaussian
mixture fraction and that here is visible as a per-hour bias
shape. The narrower question P1 leaves for Q1A and Q1B is
whether refining the conditioning $\sigma$-algebra on the
strongest-signal axis (\texttt{hour\_of\_week}) closes the
gap at the marginal-PIT level.}
\label{fig:bias-climatology}
\end{figure}

\paragraph{Q1A and Q1B (2026-05-27): does refining the
$\sigma$-algebra on the localised axis close the gap?} P1 named
\texttt{hour\_of\_week} as the binding axis. Q1A tested the
single-axis refinement: head-to-head between $Z_b$ (binary
weekend indicator) and $Z_c$ ($8$-level \texttt{hour\_of\_week}
bin) on the M3 family, with the cumulative baseline Q2A's
$\chi^{2}_{M3} = 953.84$. With paired-day bootstrap CIs:
$\chi^{2}_{Z_b} = 986.0$ $[678, 1382]$,
$\chi^{2}_{Z_c} = 952.5$ $[627, 1374]$;
$\chi^{2}_{\text{diff}} = +33.5$ $[-93, +143]$ with paired SE
$56.84$ ($Z_c$ wins at point, NEAR-TIE at $95\%$). The
\texttt{hour\_of\_week} refinement shaves $1.33$ $\chi^{2}$ off
the cumulative baseline --- $0.18\%$ of the $725.84$ gap-to-close.
The per-cell decomposition is the corroborating finding:
Saturday $\text{bin}_6$ contributes $\chi^{2} = 1634$ (single
cell exceeds the pooled marginal $952.5$ by $70\%$) at exactly
the P1 stratum at $\hat\alpha_L = 0.61$. \textbf{The contamination
is real and localised where P1 said it would be, but the
mixture-$2$ Gaussian family is at capacity within strata} ---
$Z$-stratification cannot reduce the cell's $1634$ below $228$
without the family extending, and Q2A bounded the family
extension. Q1B took the natural next step: \emph{joint}
refinement, adding \texttt{time\_of\_day} ($Z_2$, P1's
second-strongest axis at range $0.485$) on top of $Z_c$. With
$\sigma = (\text{day-type}, Z_c, Z_2)$:
$\text{better}\_\chi^{2} = 662.2$ $[468, 945]$,
$\chi^{2}_{\text{diff}} = +33.8$ $[-53, +124]$ (paired SE $44.71$,
NEAR-TIE preserved). The cumulative reduction above Q1A's $Z_c$
carryforward is $+290.3$ $[+8.06, +484.4]$ --- $40\%$ of the
remaining gap at point estimate but with a paired-day bootstrap
CI spanning a factor of $\sim 60\times$. The joint refinement
absorbs structure single-axis could not (the lower CI bound clears
Q1A's $+1.33$); the magnitude of that absorption is genuinely
uncertain. The next question this raises is methodological: how
much of that $60\times$ CI width is the SMC estimator's own
sampling variance?\footnote{Registry: Q1A =
\path{2026-05-27_q1a-z-conditioning-strongest-axis}
(\texttt{c1057bcd\ldots}; CLAIM); Q1B =
\path{2026-05-27_q1b-z2-conditioning-second-axis}
(\texttt{b3b1efa7\ldots}; CLAIM).}

\begin{figure}[h]
\centering
\includegraphics[width=\linewidth]{fig_q1b_per_cell_heatmap.pdf}
\caption{The Q1B residual is concentrated in a single cell that
P1's $\hat\alpha_L$ predicted. Per-cell $\chi^{2}$ contribution
under Q1B's winning joint stratification $\sigma = (\text{day-type},
Z_c, Z_2)$ with $Z_c$ the $8$-level \texttt{hour\_of\_week} bin
and $Z_2$ the $4$-level \texttt{time\_of\_day} bin. The framed cell
(\emph{saturday}, $Z_c = 6$ [Sat 07 -- Sun 03], $Z_2 = $ morning
ramp) at $\chi^{2} = 967$ alone exceeds the pooled marginal
$952.5$; this is exactly the stratum P1 named at $\hat\alpha_L =
0.61$. Q1A's per-cell decomposition under the coarser
$(\text{day-type}, Z_c)$ stratification put the Sat-$Z_c{=}6$ row
at $\chi^{2} = 1634$; the joint $Z_2$ refinement here splits that
$1634$ as morning ramp $967$ $>$ evening $400$ $>$ afternoon
$328$, revealing genuine within-stratum heterogeneity that
single-axis $\sigma$-algebra refinement cannot resolve. Light-grey
cells are structurally impossible $(\text{day-type}, Z_c)$
combinations (e.g., Saturday cannot fall in $Z_c \in \{0, 1, 2,
3, 4\}$).}
\label{fig:q1b-per-cell}
\end{figure}

\paragraph{Q1B$'$ (2026-05-29): where does the wide CI come
from?} Q1B's wide CI on the cumulative reduction admits two
explanations: sampling noise from the SMC particle count
($N_{\text{particles}} = 200$) or eval-window noise on the
$\sim 12{,}000$-row post-cutoff window plus paired-day bootstrap
on a $\chi^{2}$ statistic of this construction. Q1B$'$ is the
registered tightening run that discriminates: re-run Q1B's
winning configuration with $N_{\text{particles}} = 400$, all
other methodology bit-identical. The pre-registered cut at
$75\%$ of Q1B's $476.358$ $\chi^{2}$ width corresponds to the
strong-form SMC-dominance prior $V_{\text{smc}}/V_{\text{total}}
\ge 0.875$. Realised width: $506.198$ $\chi^{2}$ ($+6.3\%$ vs.\
Q1B, slightly wider, well inside paired-bootstrap noise on the
width measurement itself); point-shift $16.9$, within tolerance.
The proponent's own pre-registered falsification condition
(\textit{width $> 400 \Rightarrow$ strong-form SMC-dominance
refuted}) fires. \textbf{The wide CI is located in eval-window
noise, not estimator sampling}: SMC particle doubling does not
narrow it. The reproduction-check side-channel adds a finer
localisation: no-$Z$ pooled drift at $N = 400$ is $|\Delta| = 5.2$
$\chi^{2}$ while $Z_c$-stratified parent drift is $|\Delta| =
44.6$ (asymmetry $8.5\times$), so SMC variance \emph{is}
non-trivial at the per-stratum layer where per-cell $n$ is
$1000$--$7000$ rows, but not at the no-$Z$ pooled layer. The
$Z$-stratification is genuinely operating against the
eval-window-noise floor, not the SMC-noise floor.\footnote{Registry:
Q1B$'$ = \path{2026-05-29_q1b-prime-smc-tightening}
(\texttt{3c1954e2\ldots}; INSPECTION-only; the \texttt{.txt}
output path collided with Q1B's CLAIM-grade artifact during
emission and the artifact was restored from git \texttt{HEAD}
rather than re-emitted, demoting the registry grade. The pickle
and result.yaml carry the production-stamped numerics; the
verdict holds under the audit chain.)}

\paragraph{Where the search directs us.} The five experiments,
taken together, characterise the residual structure. The
predictive-distribution family at fixed $Q$ is at capacity (Q2A);
same-$Q$ mechanism candidates are mis-specified, with the
under-dispersion signature pointing outward (Q2B); the residual
is localised on the social-weekly grid (P1); $\sigma$-algebra
refinement on the localised axis absorbs the residual partially
but cannot fully close it (Q1A, Q1B); the wide CI on the partial
closure is intrinsic to the data, not the estimator (Q1B$'$).
The next move the search points at is the $\sigma$-algebra
extension $Q' = Q \vee \sigma(Z)$ for auxiliary $Z$ outside the
time index. The kernel framing of Section~\ref{sec:method}
accommodates this extension natively: $\kappa_Q$ becomes
$\kappa_{Q'}$, the estimator construction transfers, and the
predictive distribution conditions on the extended state.
Section~\ref{sec:auxiliary} takes up the question of which $Z$
the experiments point at and how the registered forward
experiment tests it. The under-dispersion signature, P1's
social-weekly hot-zone localisation, and the
climatology-gap-non-stationarity record (\S\ref{sec:limits})
jointly point at hourly temperature as the prior-supported
candidate; whether the data carry the closure that the kernel
framing predicts under $Q'$ is the question of the registered
forward experiment.

\section{Auxiliary information beyond the time index: the
framework-prescribed paths and the registered forward experiment}
\label{sec:auxiliary}

The directed search of Section~\ref{sec:directed-search} points
the next move at the $\sigma$-algebra extension
$Q' = Q \vee \sigma(Z)$ for auxiliary $Z$ outside the time index.
Three directions are framework-supported: (i) candidate-$Z$
testing on covariates demonstrably outside the time-indexed
$\sigma$-algebra, with hourly temperature the prior-supported
candidate per the under-dispersion signature
($\sigma^{2}_{\mathrm{iter}} < \sigma^{2}_{\mathrm{empirical}}$
at virtually every $(h, \mathrm{day\_type})$ cell from
Section~\ref{sec:directed-search}'s Q2B finding) and the
non-stationary climatology gap (Figure~\ref{fig:clim-gap-temporal});
(ii) stratified covariate-dependent two-component mixture
decomposition via NPMLEmix (Deb, Saha, Guntuboyina, Sen, JASA
2022; \texttt{arXiv:1810.07897}) as a $\sigma$-algebra adequacy
diagnostic for any proposed auxiliary $Z$; (iii) synthetic
Gaussian-DGP calibration to anchor the residual-$\chi^2$ floor
against a known reference. None of the three is pursued in this
paper; the first is the subject of the registered forward
experiment described below, and the others are the framework-
prescribed next-thread agenda.

\begin{figure}[h]
\centering
\includegraphics[width=\linewidth]{fig_clim_gap_temporal.pdf}
\caption{The post-cutoff climatology gap is non-stationary and
growing. Monthly mean of $(\mathrm{actual} - \mu_{m,h})$ across the
post-cutoff window, under both the full-history climatology
(blue) and a two-year-recency-window climatology (orange). Neither
specification absorbs the drift: by 2026 the residual sits at
$+1000$--$+1500~\mathrm{MW}$ and growing. The drift is hour-flat
(structure preserved across the $24$-hour delivery clock) and
day-type-invariant in shape. Under the kernel framing, a
non-stationary residual that no re-windowing on the same
conditioning $\sigma$-algebra can absorb is the signature of
$\sigma$-algebra under-coverage by missing exogenous content. The
direction of the drift (post-cutoff actual systematically above
the climatology mean) is consistent with the under-dispersion
signature of Section~\ref{sec:directed-search}'s Q2B finding;
hourly temperature is the prior-supported candidate $Z$.}
\label{fig:clim-gap-temporal}
\end{figure}

\subsection{Registered forward prediction experiment}
\label{sec:registered}

A fair retrospective IESO comparison is not recoverable
(Section~\ref{sec:ieso}), so the comparison is run forward as a
pre-registered experiment. The full model specification --- estimator
identity, resolved hyperparameters, data cutoff \(t_{\mathrm{cut}}\),
code commit, and a content hash of the \(\le t_{\mathrm{cut}}\)
actuals --- is committed before any forecast is issued; because the
predictor is lazy (per-anchor fit at prediction time), that
specification pins both the function and its inputs. Forecasts then
issue forward against settled actuals, scored on persistence
baselines with IESO published values recorded as differently-horizoned
context. The enforcement detail --- content-hash recheck at load, the
post-\(t_{\mathrm{cut}}\) cutoff guard, the append-only revision
ledger --- is given in Appendix~\ref{app:estimator}.

\todo[inline]{Forward-experiment results accrue with calendar time;
report once a sufficient settled window exists.}

\subsection{The retrospective IESO head-to-head is infeasible from
public archives}
\label{sec:ieso}

Can the forecast be compared retrospectively against IESO's own
published forecast on historical settled days? A fair comparison
requires two properties of an IESO archive: (i) the values reported
must be on the Ontario-demand basis (matching the forecast target
of this paper), and (ii) the values reported must be the day-ahead
issue (matching its horizon). The simpler hypothesis is that some
public IESO archive supplies both. To test it, all 133 public IESO
report directories were enumerated and each checked against the two
properties.

The simpler hypothesis fails, via two independent blockers. The
Ontario-demand-basis archive (Adequacy3
\texttt{ForecastOntDemand}) retains, for days old enough to have
settled actuals, only the end-of-delivery-day version --- so its
horizon is wrong (property (ii) violated). The day-ahead-issued
archives (DATotals, PredispTotals) publish only market totals --- so
their basis is wrong (property (i) violated): ``Total Load'' carries
a definitional \(\approx +2485~\mathrm{MW}\) offset over Ontario
Demand (MarketQuantity enumeration: Total
Energy/Loss/Load/Dispatchable/10S/10N/30R; none is Ontario demand).
No archive satisfies both. The retrospective comparison is
infeasible from public data; hence the registered forward design
of Section~\ref{sec:registered}. (Stated for public archives as of
the recorded access date.)

\section{Limitations and reproducibility}
\label{sec:limits}

\begin{itemize}
  \item \textbf{No exogenous covariates.} The predictor uses only
  past demand. Weather, calendar event flags, and similar covariates
  are out of scope.

  \item \textbf{Scope of extraction.} The data-measure semigroup
  \(\{\Pi_t\}\) exists by Chapman--Kolmogorov on the
  temporally-coherent measure (Section~\ref{sec:construction}
  qualifier 3); what is extracted is the single-step slice
  \(\widehat{\Pidelta}\), and Section~\ref{sec:multistep} iterates
  this slice. The CK consistency gate (Table~\ref{tab:gates}) reports
  that on a VAR(1) ground truth the iterate \((\widehat{\Pidelta})^h\)
  matches the directly-extractable \(\widehat{\Pi}_{h\Delta}\) within
  the gate's tolerance at \(h \le 24\); an analogous inspection-only
  probe run in-sample on the Ontario embedding finds the gap
  Frobenius-diverging with \(h\) (Figure~\ref{fig:ck-divergence}).
  Relative-Frobenius diffusion error reaches \(\approx 50\%\) at
  \(h = 24\) across day-types, against the synthetic VAR(1) baseline
  of \(\approx 6\%\) at the same horizon.

\begin{figure}[h]
\centering
\includegraphics[width=\linewidth]{fig_ck_divergence.pdf}
\caption{Chapman--Kolmogorov consistency probe (log scale).
\textit{Left}: absolute Frobenius drift error
\(\|\widehat C_{h\Delta} - (\widehat C_\Delta)^h\|_F\) of the iterate
against the directly-fit operator. \textit{Right}: relative
Frobenius diffusion error
\(\|\widehat \Sigma_{h\Delta} - \Sigma_h^{\mathrm{iter}}\|_F\,/\,
\|\Sigma_h^{\mathrm{iter}}\|_F\).}
\label{fig:ck-divergence}
\end{figure}
\end{itemize}

\paragraph{Reproducibility.} Recorded results carry two content
hashes: an inputs hash (git commit, model-specification hash, library
versions, RNG seeds, declared inputs) and a body hash recomputed at
read time. The recording refuses to write from a dirty source tree.
Exploratory development-set material is marked as such and is not
cited as a result elsewhere in the paper.

\section{Conclusion}
\label{sec:conclusion}

The kernel framing of Section~\ref{sec:method} identifies three
natural levers for reducing residual structure in $\kappa_Q$ on
univariate Ontario electricity demand: the predictive-distribution
family at fixed $Q$, refinement of $Q$ itself on time-derived
axes, and the sampling variance the SMC estimator introduces. We
measured the extent of each. The predictive-distribution-family
lever, exercised via the Student-$t$ kernel extension over the
Gaussian baseline, delivers a $16\times$ reduction in marginal-PIT
$\chi^2$ (from $22{,}328$ to $1{,}371$;
Section~\ref{sec:univariate-extent}). \textbf{This is what
univariate Ontario demand, processed under the kernel framing,
naturally affords.}

The remaining $\chi^2 \sim 1{,}371$ is the question that
Section~\ref{sec:directed-search} takes to a directed search
across the within-data lever space. Five further pre-registered
experiments characterise the residual structure: richer
distributional families at fixed $Q$ plateau at a $1.44\times$
marginal return; a per-stratum Patra--Sen diagnostic locates the
residual on \texttt{hour\_of\_week} (hot zone the weekend window,
cold zone Monday morning) and falsifies the prior-supported
seasonal/climatological framing; single-axis $\sigma$-algebra
refinement on the localised axis absorbs $0.18\%$ of the leftover
gap; joint two-axis refinement absorbs $40\%$ at point estimate
with a paired-day bootstrap CI of $[+8, +484]$ $\chi^2$ above the
single-axis baseline; doubling the SMC particle count widens that
CI by $6\%$, locating it in the eval-window noise of the
$\sim 12{,}000$-row post-cutoff window rather than in the
estimator's sampling variance. Two structural signatures emerge.
The Patra--Sen localisation is cyclical and social-weekly. The
SMC ensemble under-disperses relative to actuals at virtually
every $(h, \text{day-type})$ cell --- the canonical signature of
missing exogenous content. Together the experiments direct the
next move outward.

\textbf{The framework-prescribed extension is
$Q' = Q \vee \sigma(Z)$ for auxiliary $Z$ outside the time index}
(Section~\ref{sec:auxiliary}). The kernel framing accommodates
the extension natively --- $\kappa_Q$ becomes $\kappa_{Q'}$, the
estimator construction transfers, and the predictive distribution
conditions on the extended state. The under-dispersion signature,
the Patra--Sen social-weekly hot-zone, and the climatology-gap
non-stationarity record jointly point at hourly temperature as
the prior-supported candidate. The retrospective head-to-head
against IESO's own published forecasts was found infeasible from
public archives (Section~\ref{sec:ieso}); the comparison is run
forward as a registered prediction experiment
(Section~\ref{sec:registered}), with auxiliary-$Z$ extension to
the conditioning $\sigma$-algebra the natural next experimental
arc. The delay-embedding local-Gaussian conditional-kernel
estimator (Section~\ref{sec:method}), the $500$-day out-of-sample
backtest (Section~\ref{sec:backtest}), and the distributional
diagnostics on the SMC predictive law
(Section~\ref{sec:distributional}) are the apparatus the directed
search ran on; the framing itself is what makes the search
directed rather than exploratory.

The pre-registration / audit / arbiter / thread workflow that
produced these six experiments is part of the deliverable. The
workflow caught seven instances of the
criterion-vs-legitimate-data-structure failure mode without
retraction; in each case the arbiter pattern handled the
mechanical-vs-substantive distinction without engineering changes.
The workflow is described in the companion memo and is registered
with reproducible artifacts at
\path{notes/preregistrations/<date>_<topic>/}; the
\verb|registry verify| chain over all $14$ entries resolves clean
at the time of writing.

\appendix

\section{Estimator detail}
\label{app:estimator}

The drift \(\Cj\) (Eq.~\ref{eq:wls}) is computed as the least-squares
solution of the row-scaled system
\begin{equation}
\label{eq:rowscale}
\sqrt{W}\,X\,C \;=\; \sqrt{W}\,Y,
\qquad W = \operatorname{diag}(w_i),\ w_i \ge 0,
\end{equation}
not by forming the normal equations \((X^{\top}WX)^{-1}X^{\top}WY\):
the two coincide, and the row-scaled solve via a pseudoinverse
degrades gracefully when the scaled design is rank-deficient (a query
state with few non-negligible weights) rather than failing. The
diffusion \(\Sj\) (Eq.~\ref{eq:diff}) is the plain, unweighted
covariance of the residuals \(r_i = y_i - x_i\Cj\); \(W\) localises
which pairs enter the fit, but the second moment is taken without
further reweighting, for the collapse reason of
Section~\ref{sec:construction}. The anchor's own pair is removed from
\(\mathcal{L}\) before the fit, so \(\Cj\) and \(\Sj\) are
leave-one-out at the anchor.

The bandwidth \(\theta^{*}\) minimises the leave-one-out objective
\(\mathcal{R}_{\mathrm{LOO}}\) (Eq.~\ref{eq:loocv}) over a fixed
dimensionless grid
\begin{equation}
\label{eq:thetagrid}
\{\theta_k\}_{k=1}^{K}
\;=\; \operatorname{linspace}(0, \theta_{\max}; K),
\end{equation}
spanning the full local--global range: \(\theta = 0\) is the global
linear fit (uniform weights), and \(\theta_{\max}\) is the tightest
locality the grid resolves. True leave-one-out refits \(\Cj^{(-i)}\)
for every held-out row, at cost \(O(m^{2})\) per grid point; it is
evaluated exactly on a fixed random subsample of \(150\) library
pairs (the full library when \(|\mathcal{L}| \le 150\)), drawn from a
seeded generator for reproducibility.\footnote{The naive
refit-per-row form is used in place of a hat-matrix shortcut, which
was found to be both buggy and sensitive to the weighting convention;
the subsample evaluation is exact and costs \(\approx 0.1\,\)s per
call.} The fitting stage consumes the grid argmin directly --- no
rail-pin guard, edge fallback, or clipping. Because the S-map kernel
(Eq.~\ref{eq:weights}) carries no \(\theta\)-dependent normaliser,
the LOO objective has a well-posed left endpoint at \(\theta = 0\)
and admits the global linear operator as a finite, achievable
selection rather than an unattained limit.

Estimation at an anchor and the multi-step forecast generator of
Section~\ref{sec:caveat} call one local-fit routine, which maps a
library \((X, Y)\) and an explicit query state to
\((\Cj, \Sj, \bar r, \theta^{*}, r)\); estimation derives the query
from an embedding and an anchor, the forecast generator supplies a
post-cutoff query against a pre-cutoff library. The forecast path is
therefore the estimation path --- the backtest numbers of
Section~\ref{sec:backtest} come from the code the synthetic recovery
gate of Section~\ref{sec:synth} validates, not a re-implementation.

The forward experiment of Section~\ref{sec:registered} enforces its
committed specification mechanically. The content hash of the
\(\le t_{\mathrm{cut}}\) actuals recorded at registration is
recomputed at every prediction-time load and compared against the
stored value; any divergence raises rather than scores. A cutoff
guard rejects any row dated after \(t_{\mathrm{cut}}\) from entering
a fit. Settled actuals accumulate in an append-only ledger,
accommodating IESO's published-demand revisions as new appended
records rather than overwrites, and the ledger state is itself
hashed into each score.

\section{Per-horizon backtest table}
\label{app:backtest}

The per-horizon error structure for predictor 2 (mean-iteration,
global-OLS \(\theta = 0\)) over the \(|\mathcal{D}| = 500\)
post-cutoff delivery-day population is plotted with the other five
predictors in Figure~\ref{fig:per-horizon}; the diurnal envelope and
the hour-of-day coupling are visible there. Selected horizons for
predictor 2: \(\MAE(h{=}1) = 157.7\)~MW, \(\MAPE\,1.06\%\); peak
\(\MAE(h{=}11) = 1125.8\)~MW, \(\MAPE\,6.90\%\); decay to
\(\MAE(h{=}24) = 597.9\)~MW, \(\MAPE\,3.75\%\). One-sigma coverage
falls monotonically from \(60.4\%\) at \(h = 1\) (close to nominal)
to \(\approx 13\%\) at \(h \in [9, 13]\) and recovers to
\(\approx 18\%\) at \(h = 24\).

% ---------- References ----------
\ifstandalone
\bibliographystyle{plain}
\bibliography{references}
\fi
